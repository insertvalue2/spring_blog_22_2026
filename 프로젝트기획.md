# 📘 스프링 부트 SSR 블로그 — 학습 프로젝트 기획서

> 프로젝트명: **spring_blog_22_2026** (SSR 블로그 최종 통합 단원)
> 대상: 스프링 부트 입문~중급 학생
> 작성일: 2026-04-10
> 관련 문서: `강의계획.md` (차시별 계획), `보안.md` (코드 평가·보안 점검)

---

## 0. 이 문서는 무엇인가

학생이 코드를 한 줄 치기 전에 **“내가 무엇을, 왜 만드는지”** 를 먼저 이해할 수 있도록 만든 학습용 프로젝트 기획서입니다.

- 📗 `강의계획.md` → 강사용, **“어떤 순서로 가르칠까”**
- 📘 `프로젝트기획.md` → **학생용, “나는 지금 뭘 만들고 있는가”** (이 문서)
- 🔐 `보안.md` → 공통, **“무엇을 조심해야 하는가”**

학생은 각 챕터를 시작하기 전에 이 문서의 관련 섹션을 읽고, 전체 큰 그림 속에서 오늘 배울 기능이 어디에 붙는지 확인합니다.

---

## 1. 한 줄 소개

> **“회원이 글을 쓰고, 유료 콘텐츠를 만들고, 포인트로 결제하고, 필요하면 환불까지 받을 수 있는 풀스택 SSR 블로그 플랫폼”**

즉, 단순한 CRUD 게시판을 넘어서 **실제 서비스에서 쓰이는 도메인 복잡성**(회원/권한/결제/환불/소셜로그인)을 한 프로젝트 안에서 다뤄보는 학습 프로젝트입니다.

---

## 2. 학습 목표

### 🧠 개념 목표 (“이 수업이 끝나면 설명할 수 있어야 한다”)

학생은 수업이 끝났을 때 다음을 **자기 말로** 설명할 수 있어야 합니다.

1. SSR 방식이 CSR과 무엇이 다르고, 각자 언제 어울리는가
2. Spring MVC의 요청 → 컨트롤러 → 서비스 → 리포지토리 흐름
3. JPA의 영속성 컨텍스트, 더티 체킹, 연관관계, N+1 문제
4. 트랜잭션이 왜 필요하고, `@Transactional`이 붙는 층은 어디인가
5. 인증(Authentication)과 인가(Authorization)의 차이
6. 세션 기반 로그인의 동작 원리와 한계
7. DTO와 엔티티를 분리하는 이유
8. 비밀번호 해싱, XSS, CSRF, IDOR이 무엇이고 어떻게 막는가
9. 외부 API(카카오, PortOne)를 호출할 때 클라이언트/서버 역할 분담
10. 운영 환경으로 가기 전 점검해야 할 보안 체크리스트

### 🛠 기술 목표 (“이 기능을 직접 짤 수 있어야 한다”)

- `@Entity` + `JpaRepository`로 CRUD
- `HandlerInterceptor`로 로그인/권한 체크
- `@ControllerAdvice` + 커스텀 예외로 에러 페이지 통합
- `MultipartFile` 파일 업로드
- `Pageable`로 페이징·검색
- `@Query` / `JOIN FETCH` / 배치 페치로 N+1 해결
- `JavaMailSender`로 이메일 인증
- Summernote 리치 에디터 통합
- 외부 결제 PG(PortOne) 연동 + 서버 재검증
- OAuth 2.0(카카오) 소셜 로그인

### 🎯 태도 목표 (“이 수업이 끝나면 이런 감각을 가진다”)

- "프론트가 보낸 값은 일단 의심한다"
- "비밀은 코드에 두지 않는다"
- "돌아가는 작은 것부터 만들고, 필요해지면 개념을 도입한다"
- "동작한다 ≠ 완성됐다. 항상 '악의적으로 써보면?'을 묻는다"

---

## 3. 대상 학습자

| 항목 | 요구 수준 |
|---|---|
| Java | 클래스/메서드/상속/컬렉션/예외 처리 기본기. 람다·스트림은 써본 적 있음 |
| HTML/CSS | 기본 태그와 Bootstrap 5 기초 |
| SQL | `SELECT/INSERT/UPDATE/DELETE`, `JOIN`, `WHERE` 정도 |
| Git | `clone`, `add`, `commit`, `push`, `pull` |
| 툴 | IntelliJ IDEA Ultimate/Community 둘 다 가능 |
| **사전 요구 없음** | Spring Boot, JPA, Mustache, OAuth, PG |

> 💡 “스프링 자체는 처음이어도 괜찮지만, 자바로 클래스를 만들고 메서드를 호출하는 게 자연스러운 학생”이 이 수업의 가장 편한 시작점입니다.

---

## 4. 최종 결과물 — 화면과 기능

학생이 수업 끝에 **직접 돌려볼 수 있는** 앱의 모습입니다.

### 4-1. 주요 화면

| 화면 | 경로 | 누가 볼 수 있나 |
|---|---|---|
| 메인 / 게시글 목록 | `/`, `/board/list` | 누구나 |
| 게시글 상세 | `/board/{id}` | 누구나 (유료글은 본문 가림) |
| 게시글 작성/수정 | `/board/save`, `/board/{id}/update` | 로그인 사용자 |
| 회원가입 (이메일 인증) | `/join` | 누구나 |
| 로그인 | `/login` | 누구나 |
| 카카오 소셜 로그인 | `/user/kakao` | 누구나 |
| 마이페이지 / 회원정보 수정 | `/user/detail`, `/user/update` | 본인 |
| 포인트 충전 (결제) | `/user/charge-point` | 로그인 사용자 |
| 결제 내역 | `/user/payment-list` | 본인 |
| 구매 내역 | `/user/purchase-list` | 본인 |
| 환불 요청 | `/refund/request/{paymentId}` | 본인 (해당 결제) |
| 환불 목록 (내 환불) | `/refund/list` | 본인 |
| 관리자 대시보드 | `/admin/dashboard` | ADMIN만 |
| 관리자 환불 관리 | `/admin/refund-list` | ADMIN만 |
| 에러 페이지 | `/err/400`, `401`, `403`, `404`, `500` | 자동 이동 |

### 4-2. 주요 기능 요약

```
[회원]
 ├─ 회원가입 (이메일 인증 필수 + 프로필 이미지)
 ├─ 로컬 로그인 / 카카오 소셜 로그인
 ├─ 회원정보 수정, 프로필 이미지 관리
 └─ 마이페이지 (내 포인트, 내 글, 내 구매/결제/환불 내역)

[게시판]
 ├─ CRUD (제목/본문/작성자)
 ├─ Summernote 리치 에디터 (이미지 Base64 인라인)
 ├─ 페이징 + 제목/본문 통합 검색
 ├─ 유료 게시글 플래그 (500포인트)
 └─ 댓글 작성/삭제

[포인트·결제]
 ├─ PortOne(아임포트) 결제로 포인트 충전
 ├─ 포인트로 유료 게시글 구매 (중복 구매 방지)
 ├─ 결제 내역 조회
 └─ 환불 요청 → 관리자 승인/거절

[관리자]
 ├─ RBAC (ADMIN / USER 역할)
 ├─ 관리자 대시보드
 └─ 환불 요청 관리
```

---

## 5. 도메인 모델 (ERD)

### 5-1. 핵심 엔티티와 관계

```
                            ┌──────────────────┐
                            │     User         │
                            │ - id             │
                            │ - username (uk)  │
                            │ - password       │◄──────────┐
                            │ - email (uk)     │           │
                            │ - profileImage   │           │
                            │ - point          │           │
                            │ - provider       │           │
                            │ - createdAt      │           │
                            └────────┬─────────┘           │
                                     │                     │
                   ┌─────────────────┼──────────────┐      │
                   │                 │              │      │
          1:N      │        1:N      │      1:N     │      │
                   ▼                 ▼              ▼      │
         ┌──────────────┐   ┌───────────────┐  ┌─────────┐ │
         │  UserRole    │   │     Board     │  │ Payment │ │
         │ - id         │   │ - id          │  │ - id    │ │
         │ - role (enum)│   │ - title       │  │ - impUid│ │
         └──────────────┘   │ - content @Lob│  │ - merch.│ │
                            │ - premium     │  │ - amount│ │
                            │ - createdAt   │  │ - status│ │
                            └──────┬────────┘  └────┬────┘ │
                                   │                │      │
                         ┌─────────┼──────┐         │      │
                    1:N  │         │  1:N │     1:1 │      │
                         ▼         ▼      ▼         ▼      │
                 ┌───────────┐ ┌────────────┐ ┌──────────────┐
                 │   Reply   │ │  Purchase  │ │ RefundRequest│
                 │ - id      │ │ - id       │ │ - id         │
                 │ - comment │ │ - board    │ │ - payment    │
                 │ - board   │ │ - user ────┼─┤ - user ──────┘
                 │ - user    │ │ - createdAt│ │ - status     │
                 │ - createdAt│ └────────────┘ │ - reason     │
                 └───────────┘                │ - createdAt  │
                                              └──────────────┘
```

### 5-2. 엔티티 요약표

| 엔티티 | 무엇을 저장하나 | 핵심 관계 |
|---|---|---|
| **User** | 회원 정보, 포인트, 프로필, 로그인 제공자 | `UserRole` 1:N, `Board` 1:N, `Payment` 1:N |
| **UserRole** | 한 사용자의 역할(USER/ADMIN) | `User` N:1 |
| **Board** | 게시글 제목·본문·유료 여부 | `User` N:1, `Reply` 1:N |
| **Reply** | 댓글 | `Board` N:1, `User` N:1 |
| **Purchase** | 유료 게시글 구매 이력 | `Board` N:1, `User` N:1 |
| **Payment** | PortOne 결제 기록 | `User` N:1 |
| **RefundRequest** | 환불 요청과 상태 | `Payment` N:1 |

### 5-3. Enum

```java
Role          : USER, ADMIN
OAuthProvider : LOCAL, KAKAO
RefundStatus  : PENDING, APPROVED, REJECTED, DONE
```

---

## 6. 기능 명세 (유스케이스)

### 6-1. 비회원

- [ ] 메인/목록 보기
- [ ] 게시글 상세 보기 (무료글만 본문, 유료글은 가려짐)
- [ ] 검색·페이징 사용
- [ ] 회원가입, 로그인, 카카오 로그인

### 6-2. 회원 (USER)

- [ ] 내 프로필 조회/수정 (이미지 포함)
- [ ] 글 작성 (Summernote), 수정/삭제 — **본인 글만**
- [ ] 댓글 작성·삭제 — **본인 댓글만**
- [ ] 포인트 충전 (PortOne 결제)
- [ ] 유료 게시글 구매 (중복 방지)
- [ ] 결제/구매/환불 내역 조회 — **본인 것만**
- [ ] 환불 요청 (PENDING 상태 생성)

### 6-3. 관리자 (ADMIN)

- [ ] 관리자 대시보드 접근 (`AdminInterceptor`로 보호)
- [ ] 환불 요청 목록 조회
- [ ] 환불 승인/거절 처리 (승인 시 PortOne 환불 API 호출 + 사용자 포인트 차감)

### 6-4. 시스템이 자동으로 해야 하는 일

- 세션 만료 시 플래시 메시지 없이 `/login`으로 리다이렉트
- 비로그인 사용자가 보호된 URL에 접근 → 401 페이지
- 권한 없는 사용자가 `/admin/**` 접근 → 403 페이지
- 존재하지 않는 게시글 → 404 페이지
- 서버 오류 → 500 페이지
- 업로드된 이미지는 `/images/**` 로 정적 서빙

---

## 7. 대표 플로우 (사용자 관점)

### 7-1. 회원가입 플로우

```
 사용자                          서버
   │                              │
   │── GET /join ───────────────►│
   │◄── 회원가입 폼 ──────────────│
   │                              │
   │── POST /api/email/send ────►│── 메일 발송
   │◄── 인증번호 발송 완료 ────────│
   │                              │
   │── POST /api/email/verify ──►│── 세션에 email_verified_* 저장
   │◄── 인증 성공 ────────────────│── (회원가입 버튼 활성화)
   │                              │
   │── POST /join ──────────────►│── 세션 플래그 확인
   │                              │── BCrypt로 비밀번호 해싱
   │                              │── User 저장, 세션 플래그 제거
   │◄── redirect /login ──────────│
```

### 7-2. 유료 게시글 구매 플로우

```
 사용자                          서버
   │                              │
   │── GET /board/{id} ────────►│── 상세 조회, 구매 여부 확인
   │◄── 본문 가린 화면 + 구매버튼 ─│
   │                              │
   │── POST /board/{id}/purchase►│── 트랜잭션 시작
   │                              │   1) 사용자 포인트 차감
   │                              │   2) Purchase 레코드 저장
   │                              │── 트랜잭션 커밋
   │◄── redirect /board/{id} ─────│
   │                              │
   │── GET /board/{id} ────────►│── 구매 내역 확인 → 본문 노출
   │◄── 본문 포함 화면 ────────────│
```

### 7-3. 포인트 충전(결제) 플로우

```
 사용자(브라우저)     서버          PortOne
   │                  │              │
   │ PortOne JS SDK 호출 ─────────────┐
   │                                  │
   │◄── 결제 성공 (impUid) ───────────┘
   │                  │
   │── verify 요청 ──►│── PortOne API 호출 ──►│
   │                  │◄── 결제 정보 조회 ──────│
   │                  │── 금액/상태 검증
   │                  │── Payment 저장
   │                  │── 사용자 포인트 증가
   │◄── 완료 ─────────│
```

> ⚠️ **핵심 포인트**: 브라우저가 보낸 금액을 믿지 않고, 서버가 PortOne에 **다시 물어봐서** 검증합니다.

### 7-4. 환불 플로우

```
 사용자(본인)                 관리자                PortOne
   │                             │                    │
   │ 환불 요청 작성 ──────────────►                    │
   │ RefundRequest (PENDING)     │                    │
   │                             │                    │
   │                             │ 관리자 대시보드에서 │
   │                             │ 목록 확인          │
   │                             │                    │
   │                             │ 승인 버튼 ─────────►│
   │                             │                    │
   │                             │◄── 환불 완료 ───────│
   │                             │                    │
   │                             │ Payment 상태 변경  │
   │                             │ RefundRequest → DONE│
   │                             │ 사용자 포인트 차감  │
```

---

## 8. 기술 스택과 선택 이유

| 분류 | 기술 | 왜 이걸 썼나 (학생용 설명) |
|---|---|---|
| 언어 | Java 17 | 학교·학원 표준. LTS라 자료 풍부 |
| 프레임워크 | Spring Boot 3.5.x | 레퍼런스가 가장 많고 3.4와 거의 호환 |
| 빌드 | Gradle (Groovy) | IntelliJ 지원 강력 |
| 템플릿 | Mustache | “로직 없는” 템플릿이라 HTML에 집중 가능 |
| ORM | Spring Data JPA (Hibernate) | 개념 학습에 좋은 표준 |
| DB | MySQL 8 / H2 (로컬) | 로컬은 H2, 실전은 MySQL — 프로파일 분리 학습 |
| 비밀번호 | `spring-security-crypto` (BCrypt) | Spring Security 전체를 쓰지 않고도 해싱만 가능 |
| 메일 | `spring-boot-starter-mail` + Gmail SMTP | 별도 서비스 없이 바로 실습 |
| 리치 에디터 | Summernote (Bootstrap 5) | CDN 한 줄로 붙는 가장 가벼운 에디터 |
| 결제 PG | PortOne(아임포트) | 국내 학습용 결제 연동 사실상 표준 |
| 소셜 로그인 | 카카오 OAuth 2.0 | 국내 학생들이 가장 익숙한 제공자 |
| 프런트 UI | Bootstrap 5 | CSS 디테일에 시간 뺏기지 않도록 |

### ❓ 왜 Spring Security 전체는 안 쓰나?

> **학습 단계별로 “지금 필요한 개념만”** 도입하기 위해서.
> Security 전체를 켜면 필터 체인·설정 블록·CSRF·OAuth2가 한꺼번에 오기 때문에, 학생이 “왜 이게 필요한지”를 이해하기 전에 압도됩니다.
> 이 프로젝트에서는 **BCrypt + 인터셉터**만 써서 인증/인가의 본질을 손으로 짜보고, **Ch16(보안 회고) 단원에서** 비로소 Security 정식 도입을 체험합니다.

---

## 9. 아키텍처 개요

### 9-1. 3계층 아키텍처

```
 [Client: 브라우저]
        │ HTTP
        ▼
 ┌───────────────────────────────────────┐
 │ Interceptor 층                        │
 │  ├─ SessionInterceptor (공통)         │
 │  ├─ LoginInterceptor (/board, /user…) │
 │  └─ AdminInterceptor (/admin/**)      │
 └────────────────┬──────────────────────┘
                  ▼
 ┌───────────────────────────────────────┐
 │ Controller (요청 처리)                │
 │  - HTTP ↔ DTO                         │
 │  - 세션 읽기                          │
 │  - Service 호출                       │
 └────────────────┬──────────────────────┘
                  ▼
 ┌───────────────────────────────────────┐
 │ Service (비즈니스 + 트랜잭션 경계)    │
 │  - @Transactional                     │
 │  - 유효성/권한 검사                   │
 │  - 엔티티 ↔ DTO 변환                  │
 └────────────────┬──────────────────────┘
                  ▼
 ┌───────────────────────────────────────┐
 │ Repository (DB 접근)                  │
 │  - JpaRepository                      │
 │  - @Query (JPQL)                      │
 └────────────────┬──────────────────────┘
                  ▼
             [DB: MySQL / H2]
```

### 9-2. 패키지 구조

```
org.example.demo_ssr_v1_1
 ├─ DemoSsrV11Application.java   ← 엔트리포인트
 │
 ├─ _core/                        ← 공통 인프라
 │   ├─ config/                  ← WebMvcConfig, PasswordEncoder 빈
 │   ├─ errors/                  ← ControllerAdvice, 커스텀 예외
 │   ├─ interceptor/             ← Login/Admin/Session
 │   └─ utils/                   ← FileUtil, MailUtils, MyDateUtil
 │
 ├─ user/                         ← 회원/인증/OAuth/이메일
 ├─ board/                        ← 게시글
 ├─ reply/                        ← 댓글
 ├─ payment/                      ← PortOne 결제
 ├─ purchase/                     ← 유료글 구매
 ├─ refund/                       ← 환불 요청/관리
 └─ admin/                        ← 관리자 화면
```

> 💡 **도메인 기준 패키지 분리** — `controller/`, `service/`, `repository/` 식으로 층 기준으로 나누지 않고, **도메인(기능) 기준**으로 나눕니다. 한 기능을 이해하려고 세 폴더를 옮겨 다니지 않아도 됩니다.

### 9-3. 예외 처리 체계

| 예외 | 상황 | 응답 |
|---|---|---|
| `Exception400` | 유효성 오류, 비즈니스 실패 | `<script>alert + history.back()` |
| `Exception401` | 미로그인 | `/login` 으로 이동 |
| `Exception403` | 권한 없음 | 403 페이지 |
| `Exception404` | 리소스 없음 | 404 페이지 |
| `Exception500` | 서버 내부 오류 | 500 페이지 |

모두 `MyExceptionHandler`(`@ControllerAdvice`)에서 중앙 처리됩니다.

---

## 10. 학습 로드맵 (강의계획과 연결)

| PART | 학생이 만드는 것 | 핵심 학습 개념 | 관련 강의 챕터 |
|---|---|---|---|
| **A. 기초** | 익명 게시판 1차 (CRUD) | SSR, Mustache, JPA 기초, 더티 체킹 | Ch01~03 |
| **B. 사용자와 권한** | 회원제 게시판 + 관리자 페이지 | 세션, 인터셉터, BCrypt, 3계층, RBAC | Ch04~07 |
| **C. 풍부해지는 게시판** | 블로그 (댓글·페이징·검색·프로필·리치에디터) | 파일 업로드, N+1 해결, XSS 방어 | Ch08~11 |
| **D. 결제 도메인** | 유료 콘텐츠 플랫폼 (이메일 인증·결제·환불) | 외부 API 연동, 트랜잭션 원자성, 서버 재검증 | Ch12~14 |
| **E. 마무리** | 출시 직전 점검 (카카오 로그인·보안 회고) | OAuth 2.0, 보안 체크리스트 | Ch15~16 |

> 각 PART가 끝날 때마다 **“그 자체로 돌아가는 앱”** 이 학생 손에 있습니다. 다음 PART는 그 앱에 기능을 얹어가는 방식입니다.

---

## 11. 학생용 학습 가이드

### 11-1. 시작 전에 준비할 것

```
✔ JDK 17 설치 (IntelliJ의 내장 JDK도 가능)
✔ IntelliJ IDEA (Community 이상)
✔ Git 설치
✔ MySQL 8 (또는 Docker) — PART D부터 필요. PART A~C는 H2만으로 가능
✔ Google 계정 + 앱 비밀번호 — PART D(이메일 인증)에서 필요
✔ 카카오 디벨로퍼스 앱 등록 — PART E(소셜 로그인)에서 필요
✔ PortOne(아임포트) 테스트 계정 — PART D(결제)에서 필요
```

### 11-2. 각 챕터를 공부하는 4단계 사이클

수업이든 복습이든 **항상 같은 4단계**로 갑니다.

```
1) 왜? — 이 기능이 왜 필요한가? (5분)
2) 손으로 쳐보기 — 강사 라이브를 같은 속도로 따라 치기 (복붙 금지)
3) 부숴보기 — 일부러 에러를 내보고 원인 읽기
4) 혼자 다시 — 같은 기능을 닫고 다시 짜보기
```

### 11-3. 막혔을 때 체크리스트

```
□ 에러 메시지를 끝까지 읽었는가? (번역해서라도)
□ 콘솔의 SQL 로그를 확인했는가? (show-sql: true)
□ @Transactional이 붙어 있는가?
□ 인터셉터가 등록되어 있는가? (WebMvcConfig)
□ 세션에 sessionUser가 실제로 있는가?
□ DB의 실제 값이 내가 생각한 값과 같은가? (h2-console로 확인)
□ 엔티티의 @NoArgsConstructor가 있는가?
□ DTO의 필드명과 폼 필드명이 일치하는가?
□ 생성자/빌더에 필드를 빠뜨리지 않았는가?
```

### 11-4. 복붙하지 말아야 할 순간, 복붙해도 되는 순간

| 복붙 금지 | 복붙 OK |
|---|---|
| 컨트롤러/서비스/엔티티 본체 | application.yml 환경변수 템플릿 |
| JPA `@Query` 문자열 | Bootstrap CSS 클래스 |
| 인터셉터 `preHandle` 로직 | HTML 구조(폼 레이아웃 등) |
| 예외 처리 체계 | 에러 페이지 Mustache 템플릿 |

> 이유: **핵심 학습 포인트는 손에 익혀야 하고, 부수적 반복은 시간 낭비**입니다.

### 11-5. 제출·회고 양식 (권장)

각 PART 끝에 다음 4가지를 제출:

1. **동작 영상** (30초 — 주요 흐름 1개)
2. **내가 막혔던 순간 1개**
3. **해결한 방법 한 줄**
4. **다음 PART에서 궁금한 것 1개**

---

## 12. 완성 체크리스트

수업 끝에 학생이 스스로 점검하는 자기 평가표입니다.

### 12-1. 기능 체크리스트

#### 회원/인증
- [ ] 이메일 인증 없이는 회원가입이 실패한다
- [ ] 비밀번호가 DB에 BCrypt 해시로 저장돼 있다
- [ ] 로그인하지 않고 `/board/save`에 가면 `/login`으로 튕긴다
- [ ] `/admin/**`은 USER 역할로는 403
- [ ] 카카오 로그인으로 가입·로그인이 모두 된다
- [ ] 프로필 이미지를 업로드·변경·삭제할 수 있다

#### 게시판/댓글
- [ ] 내가 쓴 글만 수정·삭제 버튼이 보인다
- [ ] URL 변조로 남의 글을 수정·삭제할 수 없다 (403)
- [ ] 검색어가 제목/본문을 모두 뒤진다
- [ ] 페이지 이동 시 검색어가 유지된다
- [ ] Summernote에 이미지를 넣어 저장하면 상세에서 그대로 보인다
- [ ] 댓글을 달고 내 댓글만 삭제 가능하다

#### 결제/구매/환불
- [ ] 유료 게시글은 구매 전엔 본문이 가려진다
- [ ] 포인트가 부족하면 구매에 실패한다
- [ ] 중복 구매가 막힌다
- [ ] PortOne으로 포인트가 충전된다
- [ ] 결제 금액을 클라이언트가 조작해도 서버가 잡는다
- [ ] 내 결제만 환불 요청할 수 있다
- [ ] 이미 환불 진행 중이면 재요청이 막힌다
- [ ] 관리자는 환불을 승인/거절할 수 있다

### 12-2. 보안 체크리스트 (`보안.md`와 연결)

- [ ] `application-*.yml`에 비밀 값이 들어있지 않다
- [ ] `System.out.println`이 남아있지 않다
- [ ] Summernote 본문을 Jsoup 등으로 살균하고 있다
- [ ] 쿠키에 `HttpOnly`가 설정돼 있다
- [ ] 세션 로그인 직후 세션 ID가 재발급된다
- [ ] 파일 업로드가 확장자 화이트리스트 + 매직 넘버로 검증된다
- [ ] 운영 프로파일(prod)에서 H2 콘솔이 꺼져 있다
- [ ] Git 히스토리에 비밀이 들어갔다면 revoke 처리가 됐다

> ✅ **모두 체크되면 “동작하는 것”을 넘어 “운영에 올릴 수 있는 것”에 가까워진 것입니다.**

---

## 13. 발전 과제 (학기 말 보너스)

수업 범위 밖이지만, 이 프로젝트를 한 단계 더 키우고 싶은 학생을 위한 과제입니다.

1. **Spring Security 정식 도입** — 현재 인터셉터 기반을 Security Filter Chain으로 교체
2. **CSRF 토큰** — 모든 form 요청에 토큰 추가
3. **좋아요 기능** — `Board` ↔ `User` N:M 매핑 + 중복 방지
4. **태그** — 게시글에 태그 달고 태그로 검색
5. **알림** — 내 글에 댓글이 달리면 알림
6. **이미지 서버 저장** — Summernote Base64 → 서버 업로드 + URL 참조로 전환
7. **Docker + GitHub Actions** — 컨테이너화와 CI 자동 빌드
8. **Spring Boot 4.0 마이그레이션** — 현재 3.5에서 4.0으로 올리며 deprecation 해결
9. **REST API 버전** — 같은 도메인을 REST로 다시 만들어 React 프런트 연결
10. **테스트 코드** — `@DataJpaTest`, `@WebMvcTest`, `@SpringBootTest` 작성

---

## 14. 이 문서와 함께 읽어야 할 것

| 문서 | 역할 | 언제 읽는가 |
|---|---|---|
| 📘 `프로젝트기획.md` (본 문서) | “무엇을, 왜 만드는가” | **수업 시작 전 1회** + 각 PART 시작 시 |
| 📗 `강의계획.md` | “어떤 순서로 가르치는가” | 강사 전용. 학생은 로드맵만 참고 |
| 🔐 `보안.md` | “무엇을 조심해야 하는가” | Ch09(업로드), Ch11(XSS), Ch16(회고) |
| 📝 `작업내용.md` | “Summernote/이메일 인증 구현 상세” | Ch11, Ch12 보조 자료 |

---

## 15. 마무리 — 학생에게 한 장 당부

> 이 프로젝트가 끝나면 여러분은 **“게시판 만들 줄 아는 사람”에서 “도메인을 설계하고 결제까지 붙일 줄 아는 사람”** 이 됩니다.
>
> 그 과정에서 가장 중요한 건 **“지금 내가 어느 층에서 일하고 있는가”** 를 항상 의식하는 것입니다. 컨트롤러인지, 서비스인지, 엔티티인지, 뷰인지. 층이 흐려지면 코드가 흐려지고, 코드가 흐려지면 버그가 많아집니다.
>
> 막히면 에러 메시지를 정확히 읽고, 가장 작은 단위로 잘라서 확인하세요. 복붙하지 말고 손으로 치세요. 그리고 항상 **“악의적으로 써보면?”** 을 한 번 더 물어보세요.
>
> 이 세 가지만 지키면, 여러분은 이 수업을 훨씬 많이 가져갈 수 있습니다.

---

**문서 버전**: v1.0
**최종 수정**: 2026-04-10
