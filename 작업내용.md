
# 회원가입 이메일 인증 강제 기능 작업 내용

## 📋 개요

회원가입 시 이메일 인증을 완료하지 않으면 회원가입이 불가능하도록 프론트엔드와 백엔드 모두에서 검증하는 기능을 추가했습니다.
기존에는 프론트엔드 JavaScript만으로 검증하여 개발자 도구로 우회가 가능했지만, 이제 서버 측에서도 반드시 이메일 인증을 확인합니다.

---

## 🎯 주요 기능

- **프론트엔드**: 이메일 인증 완료 전까지 회원가입 버튼 비활성화
- **백엔드**: 이메일 인증 완료 시 세션에 플래그 저장
- **백엔드**: 회원가입 처리 시 서버에서 이메일 인증 여부 확인
- **보안 강화**: JavaScript 우회 불가능하도록 서버 측 검증 추가

---

## 📁 수정된 파일

```
src/main/resources/templates/user/
└── join-form.mustache                    # 회원가입 폼 (버튼 비활성화 로직 추가)

src/main/java/org/example/demo_ssr_v1_1/user/
├── UserApiController.java                 # 이메일 인증 API (세션 플래그 저장)
└── UserController.java                    # 회원가입 처리 (인증 검증 추가)
```

---

## 🔄 동작 흐름

```
1. 사용자가 회원가입 폼 접속
   - 회원가입 버튼: 비활성화 상태 (disabled)
   ↓
2. 이메일 입력 후 "인증번호 전송" 버튼 클릭
   - POST /api/email/send 호출
   - 인증번호 이메일 발송
   - 인증번호 입력창 표시
   ↓
3. 인증번호 입력 후 "확인" 버튼 클릭
   - POST /api/email/verify 호출
   ↓
4. 서버에서 인증번호 확인
   - MailService.인증번호확인() 호출
   ↓
5. 인증 성공 시
   - 세션에 "email_verified_" + 이메일 = true 저장
   - 프론트엔드: 회원가입 버튼 활성화
   ↓
6. 사용자가 회원가입 버튼 클릭
   - POST /join 호출
   ↓
7. 서버에서 이메일 인증 여부 확인
   - 세션에서 "email_verified_" + 이메일 확인
   - 인증되지 않았으면 Exception400 발생
   ↓
8. 인증 완료된 경우에만 회원가입 처리
   - UserService.회원가입() 호출
   - 세션에서 인증 플래그 제거 (일회용)
```

---

## 💻 코드 상세 설명

### 1. 프론트엔드: 회원가입 버튼 비활성화/활성화

**파일**: `join-form.mustache`

#### 변경 사항 1: 버튼에 id 추가 및 초기 비활성화

```mustache
<!-- 회원가입 버튼 -->
<button type="submit" class="btn btn-primary form-control" id="joinBtn" disabled>회원가입</button>
```

**역할**:
- 버튼에 `id="joinBtn"` 추가하여 JavaScript에서 제어 가능하도록 함
- 초기 상태를 `disabled`로 설정하여 인증 전에는 클릭 불가능

#### 변경 사항 2: 인증 성공/실패 시 버튼 상태 변경

```javascript
if (response.ok) {
    // 인증 성공
    // ... 기존 코드 ...
    // - 회원가입 버튼 활성화
    document.querySelector("#joinBtn").disabled = false;
} else {
    // 인증 실패
    // ... 기존 코드 ...
    // - 회원가입 버튼 비활성화
    document.querySelector("#joinBtn").disabled = true;
}
```

**역할**:
- 인증 성공 시: 버튼의 `disabled` 속성을 `false`로 변경하여 활성화
- 인증 실패 시: 버튼의 `disabled` 속성을 `true`로 변경하여 비활성화

**UX 개선**:
- 사용자가 인증 완료 전에는 회원가입 버튼을 클릭할 수 없어 명확한 안내 제공
- 인증 실패 시 버튼이 다시 비활성화되어 재인증 필요함을 시각적으로 표시

---

### 2. 백엔드: 이메일 인증 완료 시 세션에 플래그 저장

**파일**: `UserApiController.java`

#### 변경 사항: verifyCode() 메서드에 세션 파라미터 추가 및 플래그 저장

```java
@PostMapping("/api/email/verify")
public ResponseEntity<?> verifyCode(@RequestBody UserRequest.EmailCheckDTO reqDTO, jakarta.servlet.http.HttpSession session) {
    // ... 기존 유효성 검사 코드 ...
    
    boolean isVerified = mailService.인증번호확인(reqDTO.getEmail(), reqDTO.getCode());

    if (isVerified) {
        // 인증 성공: 세션에 인증 완료 플래그 저장
        session.setAttribute("email_verified_" + reqDTO.getEmail(), true);
        return ResponseEntity.ok().body(Map.of("message", "인증되었습니다."));
    } else {
        return ResponseEntity.badRequest().body(Map.of("message", "인증번호가 일치하지 않습니다."));
    }
}
```

**역할**:
- 인증 성공 시 세션에 `"email_verified_" + 이메일` 키로 `true` 값 저장
- 회원가입 처리 시 서버에서 이 플래그를 확인하여 인증 여부 검증

**세션 키 전략**:
- `"email_verified_" + 이메일`: 이메일 주소를 포함하여 여러 사용자가 동시에 인증해도 구분 가능
- 예: `"email_verified_test@gmail.com"`

**보안 고려사항**:
- 세션 기반이므로 브라우저별로 독립적으로 관리됨
- 세션 만료 시 인증 플래그도 함께 사라짐 (재인증 필요)

---

### 3. 백엔드: 회원가입 처리 시 이메일 인증 검증

**파일**: `UserController.java`

#### 변경 사항: joinProc() 메서드에 세션 파라미터 추가 및 인증 검증

```java
@PostMapping("/join")
public String joinProc(UserRequest.JoinDTO joinDTO, HttpSession session) {
    // 이메일 인증 완료 여부 확인
    Boolean isEmailVerified = (Boolean) session.getAttribute("email_verified_" + joinDTO.getEmail());
    
    if (isEmailVerified == null || !isEmailVerified) {
        // 이메일 인증이 완료되지 않은 경우 예외 발생
        throw new Exception400("이메일 인증을 먼저 완료해주세요.");
    }
    
    // 이메일 인증이 완료된 경우 회원가입 처리
    userService.회원가입(joinDTO);
    
    // 회원가입 완료 후 세션에서 인증 플래그 제거 (일회용)
    session.removeAttribute("email_verified_" + joinDTO.getEmail());
    
    return "redirect:/login";
}
```

**역할**:
- 회원가입 요청 시 세션에서 이메일 인증 완료 플래그 확인
- 인증되지 않았으면 `Exception400` 예외 발생하여 회원가입 차단
- 인증 완료된 경우에만 회원가입 처리 진행
- 회원가입 완료 후 세션에서 인증 플래그 제거 (일회용 보장)

**보안 강화**:
- 프론트엔드 JavaScript만으로는 우회 불가능
- 개발자 도구로 hidden 필드 조작해도 서버에서 차단됨
- JavaScript 비활성화 상태에서도 서버 측 검증으로 보호됨

**예외 처리**:
- `Exception400`: "이메일 인증을 먼저 완료해주세요." 메시지와 함께 400 Bad Request 응답
- `MyExceptionHandler`에서 처리되어 alert 창으로 사용자에게 안내

---

## 🔑 핵심 기술 포인트

### 1. 프론트엔드와 백엔드 이중 검증

**프론트엔드 (UX)**:
- 사용자 경험 개선: 인증 전에는 버튼 비활성화로 명확한 안내
- 즉시 피드백: 인증 성공/실패에 따라 버튼 상태 변경

**백엔드 (보안)**:
- 필수 검증: 서버에서 반드시 인증 여부 확인
- 우회 불가능: JavaScript 조작으로는 우회 불가능

### 2. 세션 기반 인증 상태 관리

**장점**:
- 서버 측에서 상태 관리 가능
- 브라우저별로 독립적인 세션 관리
- 세션 만료 시 자동으로 재인증 필요

**세션 키 전략**:
- `"email_verified_" + 이메일`: 이메일 주소를 포함하여 고유성 보장
- 동시 접속자 간 충돌 방지

### 3. 일회용 인증 플래그

**회원가입 완료 후 플래그 제거**:
```java
session.removeAttribute("email_verified_" + joinDTO.getEmail());
```

**목적**:
- 같은 인증으로 여러 번 회원가입하는 것 방지
- 보안 강화: 인증 플래그는 한 번만 사용 가능

---

## ✅ 테스트 시나리오

### 정상 케이스

1. **이메일 인증 후 회원가입**
   - 이메일 입력 → 인증번호 전송 → 인증번호 입력 → 인증 성공
   - 회원가입 버튼 활성화 → 회원가입 성공

2. **인증 실패 후 재시도**
   - 잘못된 인증번호 입력 → 인증 실패
   - 회원가입 버튼 비활성화 유지
   - 올바른 인증번호 입력 → 인증 성공 → 회원가입 버튼 활성화

### 예외 케이스

1. **인증 없이 회원가입 시도 (프론트엔드 우회)**
   - 개발자 도구로 hidden 필드 조작
   - JavaScript로 버튼 활성화
   - 결과: 서버에서 `Exception400` 발생하여 회원가입 차단

2. **인증 없이 직접 POST 요청**
   - 브라우저 개발자 도구나 Postman으로 직접 `/join` POST 요청
   - 결과: 서버에서 `Exception400` 발생하여 회원가입 차단

3. **세션 만료 후 회원가입 시도**
   - 인증 완료 후 시간이 지나 세션 만료
   - 회원가입 버튼 클릭
   - 결과: 서버에서 `Exception400` 발생하여 재인증 필요

---

## 🎓 학습 포인트

### 1. 프론트엔드와 백엔드의 역할 분리

**프론트엔드**:
- 사용자 경험(UX) 개선
- 즉시 피드백 제공
- 사용자 편의성 향상

**백엔드**:
- 보안 강화
- 필수 검증 수행
- 데이터 무결성 보장

### 2. 세션을 활용한 상태 관리

- 서버 측에서 상태를 관리하여 클라이언트 조작 불가능
- 세션 키 전략을 통한 고유성 보장
- 일회용 플래그로 보안 강화

### 3. 이중 검증 패턴

- 프론트엔드: 사용자 경험 개선
- 백엔드: 보안 강화
- 두 계층 모두에서 검증하여 안전성과 사용성 모두 확보

---

## 📝 참고 사항

- 이메일 인증 플래그는 세션에 저장되므로 세션 만료 시 재인증 필요
- 회원가입 완료 후 인증 플래그는 자동으로 제거되어 재사용 불가능
- 동일 이메일로 여러 번 인증해도 마지막 인증 상태만 유효
- 세션 기반이므로 브라우저별로 독립적으로 관리됨

---

**작성일**: 2024년  
**작성자**: 개발팀  
**버전**: 1.0

---

# 게시글 작성/수정에 Summernote 에디터 적용 작업 내용

## 📋 개요

게시글 작성 및 수정 화면에 Summernote 에디터(Bootstrap 5 호환)를 적용하여 리치 텍스트 에디터 기능을 제공합니다. 이미지는 Base64로 인코딩하여 HTML 콘텐츠에 직접 포함시켜 데이터베이스에 저장합니다.

---

## 🎯 주요 기능

- **Summernote 에디터 적용**: 게시글 작성/수정 폼에 WYSIWYG 에디터 추가
- **Bootstrap 5 호환**: Summernote Bootstrap 5 버전 사용
- **이미지 Base64 인코딩**: 이미지를 Base64로 변환하여 HTML에 포함
- **대용량 데이터 저장**: LONGTEXT 타입으로 Base64 이미지가 포함된 HTML 저장
- **HTML 렌더링**: 게시글 상세보기에서 HTML 콘텐츠 정상 표시

---

## 📁 수정된 파일

```
src/main/resources/templates/board/
├── save-form.mustache              # 게시글 작성 폼 (Summernote 적용)
└── update-form.mustache            # 게시글 수정 폼 (Summernote 적용)

src/main/resources/templates/board/
└── detail.mustache                 # 게시글 상세보기 (HTML 렌더링 수정)

src/main/java/org/example/demo_ssr_v1_1/board/
├── Board.java                      # @Lob 어노테이션 추가
└── BoardRepository.java            # 쿼리 수정 (JOIN FETCH 제거, LOWER() 제거)
```

---

## 🔄 동작 흐름

```
1. 사용자가 게시글 작성 페이지 접속
   - Summernote 에디터 초기화
   ↓
2. 사용자가 에디터에서 내용 작성
   - 텍스트 포맷팅, 이미지 삽입 등
   ↓
3. 이미지 업로드 시
   - FileReader API로 이미지 파일 읽기
   - Base64로 인코딩
   - data:image/jpeg;base64,... 형태로 에디터에 삽입
   ↓
4. 게시글 저장
   - Base64 이미지가 포함된 HTML이 content 필드에 저장
   - @Lob 어노테이션으로 LONGTEXT 타입으로 저장
   ↓
5. 게시글 상세보기
   - {{{board.content}}}로 HTML 렌더링
   - Base64 이미지가 포함된 콘텐츠 정상 표시
```

---

## 💻 코드 상세 설명

### 1. 게시글 작성 폼에 Summernote 적용

**파일**: `save-form.mustache`

#### 변경 사항 1: Summernote CSS/JS 추가

```mustache
<!-- Summernote Bootstrap 5 CSS -->
<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.20/dist/summernote-bs5.min.css" rel="stylesheet">
```

```javascript
<!-- Summernote Bootstrap 5 JS -->
<script src="https://cdn.jsdelivr.net/npm/summernote@0.8.20/dist/summernote-bs5.min.js"></script>
```

**역할**:
- Bootstrap 5 호환 버전의 Summernote 라이브러리 로드
- CDN을 통한 빠른 로딩

#### 변경 사항 2: textarea를 Summernote 에디터로 변경

```mustache
<div class="mb-3">
    <!-- Summernote 에디터 -->
    <textarea id="summernote" class="form-control" name="content"></textarea>
</div>
```

```javascript
$(document).ready(function() {
    $('#summernote').summernote({
        placeholder: '내용을 입력하세요',
        tabsize: 2,
        height: 400,
        lang: 'ko-KR',
        callbacks: {
            onImageUpload: function(files) {
                for (var i = 0; i < files.length; i++) {
                    insertImageAsBase64(files[i]);
                }
            }
        }
    });
});
```

**역할**:
- 기본 textarea를 Summernote 에디터로 변환
- 한국어 언어팩 설정
- 높이 400px, 탭 크기 2로 설정
- 이미지 업로드 콜백 설정

#### 변경 사항 3: 이미지 Base64 인코딩 함수

```javascript
function insertImageAsBase64(file) {
    var reader = new FileReader();
    
    reader.onload = function(e) {
        var base64Image = e.target.result;
        $('#summernote').summernote('insertImage', base64Image);
    };
    
    reader.onerror = function() {
        alert('이미지 읽기에 실패했습니다.');
    };
    
    reader.readAsDataURL(file);
}
```

**역할**:
- `FileReader` API로 이미지 파일을 읽음
- `readAsDataURL()`로 Base64로 인코딩
- `data:image/jpeg;base64,...` 형태로 에디터에 삽입
- 서버 업로드 없이 클라이언트에서 직접 처리

**장점**:
- 서버 부하 감소 (이미지 파일 저장 불필요)
- 즉시 에디터에 삽입되어 빠른 피드백
- 별도의 이미지 관리 불필요

**단점**:
- HTML 크기가 매우 커짐 (이미지 1개당 수백 KB~MB)
- 데이터베이스 저장 공간 증가
- 페이지 로딩 속도 저하 가능

---

### 2. 게시글 수정 폼에도 Summernote 적용

**파일**: `update-form.mustache`

#### 변경 사항: 작성 폼과 동일한 설정 적용

```mustache
<textarea id="summernote" class="form-control" name="content">{{{board.content}}}</textarea>
```

**역할**:
- 기존 게시글 내용을 에디터에 로드
- `{{{board.content}}}`로 HTML 이스케이프 없이 표시
- 작성 폼과 동일한 Summernote 설정 적용

---

### 3. 게시글 상세보기 HTML 렌더링 수정

**파일**: `detail.mustache`

#### 변경 사항: HTML 이스케이프 제거

```mustache
<!-- 이전 -->
{{board.content}}

<!-- 수정 후 -->
{{{board.content}}}
```

**역할**:
- Mustache의 기본 동작은 HTML을 이스케이프하여 텍스트로 표시
- `{{{...}}}` (세 개의 중괄호) 사용 시 HTML 이스케이프 없이 렌더링
- Base64 이미지가 포함된 HTML이 정상적으로 표시됨

**보안 고려사항**:
- 사용자가 입력한 HTML이 그대로 렌더링되므로 XSS 공격 위험
- Summernote는 기본적으로 안전한 HTML만 생성하지만, 추가 검증 권장

---

### 4. Board 엔티티에 @Lob 어노테이션 추가

**파일**: `Board.java`

#### 변경 사항: content 필드에 @Lob 추가

```java
@Lob // 대용량 텍스트 저장을 위한 어노테이션 (MySQL에서는 LONGTEXT 타입으로 자동 매핑)
private String content;
```

**역할**:
- `@Lob` 어노테이션으로 대용량 텍스트 저장 가능
- MySQL에서 자동으로 LONGTEXT 타입으로 매핑
- Base64 이미지가 포함된 대용량 HTML 저장 가능

**데이터베이스 타입**:
- MySQL: LONGTEXT (최대 4GB)
- PostgreSQL: TEXT
- H2: CLOB

---

### 5. BoardRepository 쿼리 수정

**파일**: `BoardRepository.java`

#### 변경 사항 1: JOIN FETCH 제거

**문제점**:
- `JOIN FETCH`와 `Page`를 함께 사용할 수 없음 (Hibernate 제약)
- 애플리케이션 시작 시 쿼리 검증 실패

**해결 방법**:
```java
// 이전 (오류 발생)
@Query(value = "SELECT DISTINCT b FROM Board b JOIN FETCH b.user ...")
Page<Board> findAllWithUserOrderByCreatedAtDesc(Pageable pageable);

// 수정 후
@Query("SELECT b FROM Board b JOIN b.user ORDER BY b.createdAt DESC")
Page<Board> findAllWithUserOrderByCreatedAtDesc(Pageable pageable);
```

**설명**:
- 일반 `JOIN`만 사용하여 페이징 가능
- `default_batch_fetch_size: 100` 설정으로 N+1 문제 완화
- 배치 페치로 여러 User를 한 번에 조회

#### 변경 사항 2: LOWER() 함수 제거

**문제점**:
- `@Lob` 필드에 `LOWER()` 함수 사용 시 타입 불일치 오류
- Hibernate가 CLOB 타입으로 인식하여 STRING 함수 사용 불가

**오류 메시지**:
```
Parameter 1 of function 'lower()' has type 'STRING', but argument is of type 'java.lang.String' mapped to 'CLOB'
```

**해결 방법**:
```java
// 이전 (오류 발생)
@Query("SELECT b FROM Board b JOIN b.user " +
       "WHERE LOWER(b.title) LIKE LOWER(CONCAT('%', :keyword, '%')) " +
       "   OR LOWER(b.content) LIKE LOWER(CONCAT('%', :keyword, '%')) " +
       "ORDER BY b.createdAt DESC")

// 수정 후
@Query("SELECT b FROM Board b JOIN b.user " +
       "WHERE b.title LIKE CONCAT('%', :keyword, '%') " +
       "   OR b.content LIKE CONCAT('%', :keyword, '%') " +
       "ORDER BY b.createdAt DESC")
```

**설명**:
- MySQL의 기본 collation이 case-insensitive이므로 `LOWER()` 없이도 대소문자 구분 없이 검색
- JPQL로 사용 가능 (Native Query 불필요)
- `@Lob` 필드에서도 `LIKE`만 사용하면 문제 없음

---

## 🔑 핵심 기술 포인트

### 1. Base64 이미지 인코딩

**장점**:
- 서버에 이미지 파일 저장 불필요
- 별도의 이미지 관리 로직 불필요
- 즉시 에디터에 삽입되어 빠른 피드백

**단점**:
- HTML 크기가 매우 커짐
- 데이터베이스 저장 공간 증가
- 페이지 로딩 속도 저하 가능

**대안**:
- 이미지 파일을 서버에 저장하고 URL만 포함하는 방식 (기존 구현)
- 클라우드 스토리지 사용 (S3, Cloudinary 등)

### 2. @Lob 어노테이션

**역할**:
- 대용량 텍스트 저장을 위한 JPA 어노테이션
- 데이터베이스별로 적절한 타입으로 자동 매핑
- MySQL: LONGTEXT (최대 4GB)

**주의사항**:
- `@Lob` 필드에 JPQL 함수 사용 시 제약이 있을 수 있음
- `LOWER()`, `UPPER()` 등은 CLOB 타입에서 사용 불가
- `LIKE` 연산자는 사용 가능

### 3. JOIN FETCH vs 일반 JOIN + 배치 페치

**JOIN FETCH**:
- 한 번의 쿼리로 연관 엔티티까지 함께 조회
- 페이징과 함께 사용 불가능
- N+1 문제 완전 해결

**일반 JOIN + 배치 페치**:
- 페이징과 함께 사용 가능
- 배치 페치로 N+1 문제 완화
- 여러 쿼리로 나뉘지만 성능 저하 최소화

**선택 기준**:
- 페이징이 필요하면: 일반 JOIN + 배치 페치
- 페이징이 불필요하면: JOIN FETCH

### 4. Mustache HTML 렌더링

**이스케이프 동작**:
- `{{variable}}`: HTML 이스케이프 (기본)
- `{{{variable}}}`: HTML 이스케이프 없음 (세 개의 중괄호)

**사용 시나리오**:
- 사용자 입력: `{{variable}}` (XSS 방지)
- 에디터 콘텐츠: `{{{variable}}}` (HTML 렌더링)

---

## ✅ 테스트 시나리오

### 정상 케이스

1. **게시글 작성**
   - Summernote 에디터에서 텍스트 작성
   - 이미지 삽입 → Base64로 변환되어 에디터에 표시
   - 게시글 저장 → DB에 HTML 저장

2. **게시글 수정**
   - 기존 게시글 내용이 에디터에 로드됨
   - 이미지 포함된 HTML이 정상 표시
   - 수정 후 저장

3. **게시글 상세보기**
   - Base64 이미지가 포함된 HTML이 정상 렌더링
   - 이미지가 정상적으로 표시됨

### 예외 케이스

1. **대용량 이미지 업로드**
   - 이미지가 너무 크면 Base64 인코딩 후 HTML 크기가 매우 커짐
   - DB 저장 공간 증가
   - 페이지 로딩 속도 저하

2. **이미지 읽기 실패**
   - `FileReader.onerror`에서 에러 처리
   - 사용자에게 알림 표시

---

## 🎓 학습 포인트

### 1. WYSIWYG 에디터 통합

- Summernote와 같은 에디터 라이브러리 사용법
- 콜백 함수를 통한 커스터마이징
- 이미지 업로드 처리 방법

### 2. Base64 인코딩

- `FileReader` API 사용법
- Base64 인코딩/디코딩
- Data URL 형식 이해

### 3. JPA @Lob 어노테이션

- 대용량 데이터 저장 방법
- 데이터베이스별 타입 매핑
- JPQL 함수 사용 제약사항

### 4. 쿼리 최적화

- JOIN FETCH vs 일반 JOIN
- 배치 페치를 통한 N+1 문제 해결
- 페이징과 함께 사용하는 방법

---

## 📝 참고 사항

- Summernote는 Bootstrap 5 호환 버전 사용 (summernote-bs5.js)
- 이미지는 Base64로 인코딩되어 HTML에 직접 포함
- 데이터베이스의 content 컬럼은 LONGTEXT 타입으로 저장
- 게시글 상세보기에서 `{{{board.content}}}`로 HTML 렌더링
- MySQL의 기본 collation이 case-insensitive이므로 `LOWER()` 함수 불필요
- `JOIN FETCH`는 페이징과 함께 사용할 수 없으므로 일반 JOIN 사용

---

**작성일**: 2025년 1월  
**작성자**: 개발팀  
**버전**: 1.0