---
name: spring-boot-ssr-style
description: spring_blog_22_2026 프로젝트의 코드 스타일(도메인 기준 패키지, Controller/Service/Repository 계층, 한글 메서드명, Exception400~500 예외 체계, DTO 내부클래스 묶음, Mustache SSR, .env 환경변수)을 그대로 새 스프링 부트 프로젝트에 적용합니다. 학생 학습용 예제 프로젝트에 사용.
---

# 🧭 이 파일은 무엇인가?

이 파일은 `spring_blog_22_2026` 프로젝트에서 사용한 **코드 스타일 규칙**을 클로드에게 알려주는 지시문입니다.

**언제 쓰나?**
- 새 스프링 부트 프로젝트를 시작할 때
- "무인 편의점" 같은 순수 자바 프로젝트를 스프링 부트로 다시 만들 때
- 다른 사람의 스프링 프로젝트를 이 코드 스타일에 맞춰 리팩토링할 때

---

# 🚀 사용법 — 2가지 방법

## 방법 1. 프롬프트로 바로 붙여넣기 (가장 쉬움)

1. 새 프로젝트 폴더에서 클로드 코드를 실행
2. 아래 `--- 여기서부터 복사 ---` 줄 아래 내용을 **전부 복사**
3. 첫 메시지로 붙여넣고, 맨 아래에 "이 스타일로 ○○ 프로젝트 만들어줘" 라고 추가

예시:
```
(아래 코드 스타일 규칙 전부 붙여넣기)

이 스타일로 "무인 편의점 재고 관리 시스템" 을 스프링 부트로 만들어줘.
도메인: product, admin, sales.
```

## 방법 2. 클로드 코드 스킬로 등록 (한 번 설정하면 계속 편함)

1. 윈도우 탐색기에서 아래 폴더로 이동 (없으면 만들기):
   ```
   C:\Users\dnote\.claude\skills\spring-boot-ssr-style\
   ```
   > 💡 `.claude` 폴더는 숨김일 수 있음. 주소창에 직접 경로 치면 들어가짐.

2. 이 파일(`클로드_스프링부트_코드스타일.md`)을 위 폴더에 복사
3. 파일명을 **`SKILL.md`** 로 변경
4. 클로드 코드 재시작
5. 이제 어떤 프로젝트에서든 이렇게 부를 수 있음:
   ```
   스프링부트 SSR 코드 스타일로 무인 편의점 프로젝트 만들어줘
   ```
   또는 `/skills` 명령으로 목록에서 선택

> 🔍 **스킬이란?** 클로드에게 "이 문서를 필요할 때 읽고 그대로 따라 해" 라고 등록해두는 기능.
> 매번 긴 프롬프트를 복붙할 필요가 없어짐.

---

# --- 여기서부터 복사 ---

# 📘 Spring Boot SSR 코드 스타일 규칙

당신은 `spring_blog_22_2026` 레퍼런스 프로젝트의 코드 스타일대로 스프링 부트 SSR 프로젝트를 만드는 조력자입니다.
이 문서의 모든 규칙을 **따라야 하며**, 사용자 요청과 충돌하지 않는 한 빠뜨리지 않습니다.

---

## 1. 대원칙

1. **학생 학습용 예제** 가 기본 맥락이다. 화려한 설계보다 "이해하기 쉬운 전형적인 패턴" 을 우선한다.
2. **도메인 기준 패키지 분리** 를 절대 지킨다. `controller/`, `service/` 처럼 계층 기준으로 나누지 않는다.
3. **Controller → Service → Repository** 3계층을 엄격히 지킨다. Controller 에서 Repository 를 직접 호출하지 않는다.
4. **엔티티는 컨트롤러/뷰에 직접 노출하지 않는다**. 항상 Request/Response DTO 로 감싸서 주고받는다.
5. **Spring Security 전체는 도입하지 않는다**. 인증은 세션 + 인터셉터로, 해싱만 `spring-security-crypto(BCrypt)` 를 쓴다.
6. **한글 메서드명 허용**. Service 의 비즈니스 메서드는 한글(`회원가입()`, `로그인()`)로 쓴다. 도메인 메서드(`update()`, `isOwner()`)는 영어.
7. **주석은 "흐름" 중심**. 복잡한 메서드엔 `[동작 흐름] 1) 2) 3) ...` 패턴을 쓴다. "왜" 를 설명하고 "무엇" 은 코드 자체로 보이게 한다.
8. **System.out.println 금지**. 모든 로깅은 `@Slf4j` + `log.debug/info/error` 로. 토큰·비밀번호는 절대 로그에 찍지 않는다.

---

## 2. 기술 스택 (기본 세트)

사용자가 명시하지 않으면 아래를 기본으로 한다.

```
- Java 21 (LTS)
- Spring Boot 3.5.x
- Gradle (Groovy)
- Mustache (SSR 템플릿)
- Spring Data JPA (Hibernate)
- MySQL 8 (운영) / H2 (로컬 학습)
- Lombok
- spring-security-crypto (BCrypt 해싱만)
- spring-boot-starter-mail (이메일 인증이 필요할 때만)
- me.paulschwarz:spring-dotenv:4.0.0 (.env 파일 자동 로딩)
- Bootstrap 5 (CDN)
```

### build.gradle 기본 형태
```gradle
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.13'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'org.example'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
    sourceCompatibility = '21'
    targetCompatibility = '21'
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-mustache'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.security:spring-security-crypto'
    implementation 'me.paulschwarz:spring-dotenv:4.0.0'

    compileOnly 'org.projectlombok:lombok'
    runtimeOnly 'com.h2database:h2'
    runtimeOnly 'com.mysql:mysql-connector-j'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    useJUnitPlatform()
}
```

---

## 3. 패키지 구조 (필수)

**도메인 기준** 으로 나눈다. 레이어 기준(`controller/`, `service/`, `repository/`) 은 사용 금지.

```
src/main/java/org/example/{앱이름}
├── {AppName}Application.java            ← 엔트리포인트
│
├── _core/                                ← 공통 인프라 (밑줄 시작: 도메인과 구분)
│   ├── config/
│   │   └── WebMvcConfig.java            ← 인터셉터, 정적 리소스, PasswordEncoder 빈
│   ├── errors/
│   │   ├── MyExceptionHandler.java      ← @ControllerAdvice
│   │   └── exception/
│   │       ├── Exception400.java
│   │       ├── Exception401.java
│   │       ├── Exception403.java
│   │       ├── Exception404.java
│   │       └── Exception500.java
│   ├── interceptor/
│   │   ├── SessionInterceptor.java      ← 모든 요청에 sessionUser 주입
│   │   ├── LoginInterceptor.java        ← 로그인 여부 검사
│   │   └── AdminInterceptor.java        ← ADMIN 권한 검사
│   └── utils/
│       ├── FileUtil.java                ← 필요할 때만
│       ├── MyDateUtil.java              ← 필요할 때만
│       └── MailUtils.java               ← 필요할 때만
│
├── user/                                 ← 도메인별로 이렇게 묶음
│   ├── User.java                        (Entity)
│   ├── Role.java                        (Enum, 있으면)
│   ├── UserRole.java                    (Entity, 있으면)
│   ├── UserRepository.java
│   ├── UserRequest.java                 (요청 DTO 묶음)
│   ├── UserResponse.java                (응답 DTO 묶음)
│   ├── UserController.java
│   └── UserService.java
│
├── board/                                ← 다른 도메인도 같은 패턴
│   ├── Board.java
│   ├── BoardRepository.java
│   ├── BoardRequest.java
│   ├── BoardResponse.java
│   ├── BoardController.java
│   └── BoardService.java
│
└── admin/                                ← 관리자 페이지 전용 (Entity 없음)
    └── AdminController.java
```

### 리소스 구조
```
src/main/resources/
├── application.yml                       ← profiles.active 만 지정
├── application-local.yml                 ← H2 로컬
├── application-dev.yml                   ← MySQL 개발
├── db/
│   └── data.sql                          ← 시드 데이터
├── static/                               ← css, js, 이미지
└── templates/
    ├── layout/
    │   ├── header.mustache
    │   └── footer.mustache
    ├── err/
    │   ├── 400.mustache
    │   ├── 401.mustache
    │   ├── 403.mustache
    │   ├── 404.mustache
    │   └── 500.mustache
    ├── index.mustache
    └── {domain}/
        ├── list.mustache
        ├── detail.mustache
        └── save-form.mustache
```

---

## 4. 엔티티(Entity) 규칙

```java
@Data                              // 학습용 단순화 - 실전은 @Getter + 의미있는 setter 만
@NoArgsConstructor
@Table(name = "board_tb")          // 테이블명은 반드시 _tb 접미사
@Entity
public class Board {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String title;

    @Lob                           // 긴 본문은 @Lob
    private String content;

    @ManyToOne(fetch = FetchType.LAZY)   // 관계는 항상 LAZY
    @JoinColumn(name = "user_id")
    private User user;

    @ColumnDefault("false")
    private Boolean premium = false;

    @CreationTimestamp             // 생성 시각은 Hibernate 가 자동으로
    private Timestamp createdAt;

    @Builder                       // 생성자는 @Builder 로
    public Board(String title, String content, User user, Boolean premium) {
        this.title = title;
        this.content = content;
        this.user = user;
        this.premium = (premium != null) ? premium : false;
    }

    // 도메인 메서드 1: 상태 변경은 메서드로
    public void update(BoardRequest.UpdateDTO updateDTO) {
        updateDTO.validate();
        this.title = updateDTO.getTitle();
        this.content = updateDTO.getContent();
    }

    // 도메인 메서드 2: 본인 확인 (IDOR 방어)
    public boolean isOwner(Long userId) {
        if (this.user == null || userId == null) return false;
        return this.user.getId().equals(userId);
    }
}
```

**규칙**
- 테이블명은 `도메인_tb` 형식 (예: `user_tb`, `board_tb`, `reply_tb`)
- 관계는 **항상 LAZY**
- 생성자는 `@Builder` 로 받는다
- **상태 변경은 반드시 도메인 메서드로** (`update()`, `deductPoint()`, `addRole()`). setter 직접 호출 금지.
- **본인 확인 메서드 `isOwner(Long userId)`** 는 모든 소유권 있는 엔티티에 둔다.
- enum 은 `@Enumerated(EnumType.STRING)` 반드시 지정

---

## 5. Repository 규칙

```java
@Repository
public interface BoardRepository extends JpaRepository<Board, Long> {

    // 1) 단순한 건 쿼리 메서드로
    Optional<Board> findByTitle(String title);

    // 2) 상세 조회는 JOIN FETCH 로 (N+1 방지)
    @Query("SELECT b FROM Board b JOIN FETCH b.user WHERE b.id = :id")
    Optional<Board> findByIdWithUser(@Param("id") Long id);

    // 3) 페이징은 일반 JOIN + 배치 페치 (JOIN FETCH + Pageable 불가)
    @Query("SELECT b FROM Board b JOIN b.user ORDER BY b.createdAt DESC")
    Page<Board> findAllWithUserOrderByCreatedAtDesc(Pageable pageable);

    // 4) 검색은 반드시 @Param (문자열 결합 절대 금지)
    @Query("SELECT b FROM Board b JOIN b.user "
            + "WHERE b.title LIKE CONCAT('%', :keyword, '%') "
            + "   OR b.content LIKE CONCAT('%', :keyword, '%') "
            + "ORDER BY b.createdAt DESC")
    Page<Board> findByTitleContainingOrContentContaining(
            @Param("keyword") String keyword, Pageable pageable);
}
```

**규칙**
- `JpaRepository<Entity, Long>` 상속
- 복잡한 건 `@Query` + JPQL
- **문자열 결합으로 쿼리 만들지 않기** (SQL Injection)
- N+1 방지 : 상세는 JOIN FETCH, 목록 페이징은 배치 페치 + 일반 JOIN
- `default_batch_fetch_size: 100` 은 `application-*.yml` 에서 설정

---

## 6. DTO 규칙 — 내부 클래스로 묶기

파일을 쪼개지 말고 `UserRequest.java`, `UserResponse.java` 로 **한 파일에 모아둔다**.

```java
public class UserRequest {

    @Data
    public static class LoginDTO {
        private String username;
        private String password;

        public void validate() {
            if (username == null || username.trim().isEmpty()) {
                throw new Exception400("사용자명을 입력해주세요");
            }
            if (password == null || password.trim().isEmpty()) {
                throw new Exception400("비밀번호를 입력해주세요");
            }
        }
    }

    @Data
    public static class JoinDTO {
        private String username;
        private String password;
        private String email;
        private MultipartFile profileImage;  // 선택적 파일 업로드

        public void validate() {
            // ...
        }

        // DTO → Entity 변환은 DTO 쪽에 둔다
        public User toEntity(String profileImageFilename) {
            return User.builder()
                    .username(this.username)
                    .password(this.password)
                    .email(this.email)
                    .profileImage(profileImageFilename)
                    .build();
        }
    }

    @Data
    public static class UpdateDTO {
        private String password;
        public void validate() { /* ... */ }
    }
}
```

```java
public class UserResponse {

    @Data
    public static class DetailDTO {
        private Long id;
        private String username;
        private String email;

        public DetailDTO(User user) {
            this.id = user.getId();
            this.username = user.getUsername();
            this.email = user.getEmail();
        }
    }

    @Data
    public static class ListDTO {
        // ...
        public ListDTO(User user) { /* ... */ }
    }
}
```

**규칙**
- `{도메인}Request` 에 요청 DTO 들을 내부 클래스로
- `{도메인}Response` 에 응답 DTO 들을 내부 클래스로
- 각 DTO 는 `@Data` + 생성자에서 엔티티 → DTO 변환
- 요청 DTO 는 `validate()` 메서드를 가진다 (Controller 진입 시 바로 호출 가능)
- **엔티티를 직접 Controller 에 반환하지 않는다.** 항상 DTO 로 감싼다.

---

## 7. Service 규칙

```java
@Slf4j
@Service
@RequiredArgsConstructor
public class UserService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;

    // 쓰기 메서드는 @Transactional
    @Transactional
    public User 회원가입(UserRequest.JoinDTO joinDTO) {
        // 1. DTO 검증
        joinDTO.validate();

        // 2. 중복 검사
        if (userRepository.findByUsername(joinDTO.getUsername()).isPresent()) {
            throw new Exception400("이미 존재하는 사용자 이름입니다");
        }

        // 3. 비밀번호 해싱
        String hashPwd = passwordEncoder.encode(joinDTO.getPassword());

        // 4. Entity 변환 및 저장
        User user = joinDTO.toEntity(null);
        user.setPassword(hashPwd);
        return userRepository.save(user);
    }

    // 읽기 메서드는 readOnly=true
    @Transactional(readOnly = true)
    public User 로그인(UserRequest.LoginDTO loginDTO) {
        loginDTO.validate();

        User user = userRepository.findByUsernameWithRoles(loginDTO.getUsername())
                .orElseThrow(() -> new Exception400("사용자명 또는 비밀번호가 올바르지 않습니다"));

        if (!passwordEncoder.matches(loginDTO.getPassword(), user.getPassword())) {
            throw new Exception400("사용자명 또는 비밀번호가 올바르지 않습니다");
        }

        return user;
    }
}
```

**규칙**
- **클래스명은 영어, 메서드명은 한글**. (`UserService.회원가입()`)
- `@Slf4j` 로 로거, `@RequiredArgsConstructor` 로 의존성 주입
- 쓰기 → `@Transactional`, 읽기 → `@Transactional(readOnly = true)`
- **인가 검사는 반드시 Service 에서** (Controller 에서 검사하지 말 것)
- 로직 흐름은 `// 1.`, `// 2.`, `// 3.` 주석으로 번호 매기기
- 메서드 위에 `[동작 흐름]` 주석 블록으로 전체 흐름 요약
- 예외는 Exception400/403/404 를 상황에 맞게 사용

---

## 8. Controller 규칙

```java
@RequiredArgsConstructor
@Controller
public class BoardController {

    private final BoardService boardService;
    private final ReplyService replyService;

    /**
     * 게시글 상세.
     *
     * [동작 흐름]
     *  1) 세션 사용자 조회 (비로그인 허용이므로 null 가능)
     *  2) Service 에 상세 조회 위임
     *  3) 본인 글 여부 계산 (수정/삭제 버튼 노출용)
     *  4) 댓글 목록도 함께 조회
     */
    @GetMapping("/board/{id}")
    public String detail(@PathVariable Long id, Model model, HttpSession session) {
        // 1. 세션 사용자 꺼내기
        User sessionUser = (User) session.getAttribute("sessionUser");
        Long sessionUserId = (sessionUser != null) ? sessionUser.getId() : null;

        // 2. Service 위임
        BoardResponse.DetailDTO board = boardService.게시글상세조회(id, sessionUserId);

        // 3. 본인 여부
        boolean isOwner = sessionUser != null
                && board.getUserId() != null
                && board.getUserId().equals(sessionUser.getId());

        // 4. 모델 세팅
        model.addAttribute("board", board);
        model.addAttribute("isOwner", isOwner);
        return "board/detail";
    }

    @PostMapping("/board/save")
    public String saveProc(BoardRequest.SaveDTO saveDTO, HttpSession session) {
        User sessionUser = (User) session.getAttribute("sessionUser");
        boardService.게시글작성(saveDTO, sessionUser);
        return "redirect:/";
    }
}
```

**규칙**
- `@RequiredArgsConstructor` + `@Controller` (JSON API 는 `@RestController`)
- **세션에서 꺼낸 User 는 id 만 Service 에 전달** (엔티티 통째로 넘기면 detached 이슈)
- **비즈니스 로직 금지**. Service 호출만.
- POST 후에는 반드시 `redirect:` (PRG 패턴)
- 메서드명은 영어 (`detail`, `saveProc`, `updateProc`, `delete`)
- 메서드 위에 `[동작 흐름]` 주석
- JSON API 는 별도 파일 (`UserApiController.java`) 로 분리

---

## 9. 예외 체계

### 예외 클래스 5개 (고정)
```java
// _core/errors/exception/Exception400.java
public class Exception400 extends RuntimeException {
    public Exception400(String msg) { super(msg); }
}
// 401, 403, 404, 500 도 같은 패턴
```

### 전역 처리기
```java
@ControllerAdvice
@Slf4j
public class MyExceptionHandler {

    @ExceptionHandler(Exception400.class)
    @ResponseBody
    public ResponseEntity<String> ex400(Exception400 e, HttpServletRequest request) {
        logException("400", request, e);
        String message = (e.getMessage() != null) ? e.getMessage() : "잘못된 요청입니다";
        String script = buildAlertBackScript(message);
        return ResponseEntity.status(HttpStatus.BAD_REQUEST)
                .contentType(MediaType.TEXT_HTML)
                .body(script);
    }

    @ExceptionHandler(Exception401.class)
    public ResponseEntity<String> ex401(Exception401 e, HttpServletRequest request) {
        logException("401", request, e);
        String message = (e.getMessage() != null) ? e.getMessage() : "인증이 필요합니다";
        String escaped = message.replace("'", "\\'");
        String script = "<script>alert('" + escaped + "');location.href='/login';</script>";
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                .contentType(MediaType.TEXT_HTML)
                .body(script);
    }

    // 403: alert + history.back()
    // 404, 500: err/*.mustache 로 forward
    // ...

    private String buildAlertBackScript(String message) {
        String escaped = message.replace("'", "\\'");
        return "<script>alert('" + escaped + "');history.back();</script>";
    }

    private void logException(String code, HttpServletRequest request, Throwable e) {
        log.warn("=== {} 에러 발생 ===", code);
        log.warn("요청 URL : {}", request.getRequestURL());
        log.warn("에러 메세지 : {}", e.getMessage());
    }
}
```

**규칙**
- 예외 5개 (400/401/403/404/500) 전부 만들기. 필요 없어 보여도 일단 다 만든다.
- 400/401/403 은 alert 스크립트로 응답 (학습용 즉각 피드백)
- 404/500 은 `err/{code}.mustache` 페이지로 forward
- 작은따옴표 이스케이프는 반드시 `message.replace("'", "\\'")`

---

## 10. 인터셉터 3종 세트

### LoginInterceptor — 로그인 필요 URL 차단
```java
@Component
public class LoginInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        HttpSession session = request.getSession(false);
        User sessionUser = (session != null) ? (User) session.getAttribute("sessionUser") : null;
        if (sessionUser == null) {
            throw new Exception401("로그인 먼저 해주세요");
        }
        return true;
    }
}
```

### AdminInterceptor — ADMIN 권한 검사
```java
@Component
public class AdminInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        HttpSession session = request.getSession(false);
        User sessionUser = (session != null) ? (User) session.getAttribute("sessionUser") : null;
        if (sessionUser == null) throw new Exception403("로그인이 필요합니다");
        if (!sessionUser.isAdmin()) throw new Exception403("관리자 권한이 필요합니다");
        return true;
    }
}
```

### SessionInterceptor — 모든 뷰에 sessionUser 주입
```java
@Component
public class SessionInterceptor implements HandlerInterceptor {
    @Override
    public void postHandle(HttpServletRequest request, HttpServletResponse response,
                           Object handler, ModelAndView modelAndView) {
        if (modelAndView == null) return;
        HttpSession session = request.getSession(false);
        if (session == null) return;
        User sessionUser = (User) session.getAttribute("sessionUser");
        if (sessionUser == null) return;
        modelAndView.addObject("sessionUser", sessionUser);
    }
}
```

### WebMvcConfig 등록
```java
@Configuration
@RequiredArgsConstructor
public class WebMvcConfig implements WebMvcConfigurer {
    private final LoginInterceptor loginInterceptor;
    private final SessionInterceptor sessionInterceptor;
    private final AdminInterceptor adminInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(sessionInterceptor).addPathPatterns("/**");

        registry.addInterceptor(loginInterceptor)
                .addPathPatterns("/board/**", "/user/**", "/admin/**")
                .excludePathPatterns("/login", "/join", "/logout",
                        "/board/list", "/", "/board/{id:\\d+}",
                        "/css/**", "/js/**", "/images/**", "/favicon.ico",
                        "/h2-console/**");

        registry.addInterceptor(adminInterceptor).addPathPatterns("/admin/**");
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

**규칙**
- 인터셉터 3종 이름 고정
- 등록 순서도 고정: Session → Login → Admin
- `getSession(false)` 를 써서 불필요한 세션 생성 방지

---

## 11. application.yml 설정

### application.yml (프로파일 지정만)
```yaml
spring:
  profiles:
    active:
      - dev
```

### application-local.yml (H2 로컬 학습용)
```yaml
server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      force: true

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations:
        - classpath:db/data.sql
      encoding: UTF-8
```

### application-dev.yml (MySQL + .env)
```yaml
server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      force: true
  session:
    tracking-modes: cookie

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/myapp?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
```

**규칙 — 절대 건드리지 말 것**
- `open-in-view: false` (OSIV off — Service 에서 DTO 변환 강제)
- `default_batch_fetch_size: 100` (N+1 완화)
- 로컬은 H2, 개발은 MySQL
- **서버 포트는 8080** (80 쓰지 말 것 — Windows 권한 문제)

---

## 12. 환경변수(.env) 규칙

### 파일 3종
1. `.env` — 실제 값 (Git ignore)
2. `.env.example` — 팀 공유용 템플릿 (Git commit)
3. `.gitignore` — `.env` 반드시 포함

### .env 예시
```bash
DB_URL=jdbc:mysql://localhost:3306/myapp?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=your-password

# 필요 시 추가
MAIL_USERNAME=your@gmail.com
MAIL_PASSWORD=your-app-password
```

### yml 에서 사용
```yaml
password: ${DB_PASSWORD}              # 필수
username: ${DB_USERNAME:root}         # 기본값 있음
```

**규칙**
- 비밀은 **전부 `.env`**. 코드/yml에 하드코딩 절대 금지.
- `spring-dotenv` 의존성을 추가해 자동 로딩
- `.env.example` 은 반드시 제공

---

## 13. Mustache 템플릿 규칙

```mustache
{{> layout/header}}

<div class="container p-5">
    {{#sessionUser}}
        <p>{{sessionUser.username}}님 환영합니다</p>
    {{/sessionUser}}

    {{#isOwner}}
        <a href="/board/{{board.id}}/update" class="btn btn-warning">수정</a>
        <form action="/board/{{board.id}}/delete" method="post">
            <button class="btn btn-danger">삭제</button>
        </form>
    {{/isOwner}}

    <h2>{{board.title}}</h2>          {{! 이스케이프 (안전) }}
    <div>{{{board.content}}}</div>    {{! 언이스케이프 (HTML 렌더링, XSS 주의) }}
</div>

{{> layout/footer}}
```

**규칙**
- `layout/header.mustache`, `layout/footer.mustache` partial 사용
- 모든 페이지는 `{{> layout/header}}` 로 시작, `{{> layout/footer}}` 로 끝
- `{{변수}}` 는 자동 이스케이프 (안전), `{{{변수}}}` 는 HTML 렌더링 (주의)
- Bootstrap 5 CDN 사용
- 에러 페이지 5종 (`err/400.mustache` ~ `err/500.mustache`) 반드시 생성

---

## 14. 주석 스타일 — 가장 중요한 규칙

### 클래스 상단
```java
/**
 * Board 도메인 Service (비즈니스 계층)
 *
 * [계층 구조]
 *   Controller → Service → Repository → DB
 *
 * [@Transactional 규칙]
 *  · 쓰기 작업 → @Transactional
 *  · 읽기 전용 → @Transactional(readOnly = true)
 *
 * [학습 포인트]
 *  · OSIV false 환경에서는 Service 트랜잭션 안에서 DTO 로 변환해야 한다.
 */
```

### 메서드 상단
```java
/**
 * 게시글 상세 조회 + 구매 여부 확인.
 *
 * [동작 흐름]
 *  1) JOIN FETCH 로 Board + User 한 방 조회
 *  2) 없으면 404
 *  3) 로그인 사용자가 있으면 "유료 글 구매 여부" 를 확인
 *  4) DetailDTO 로 변환 (구매 여부 포함)
 */
```

### 메서드 안 (번호 주석)
```java
public void 환불승인(Long refundRequestId) {
    // 1. 조회
    RefundRequest refundRequest = ...;

    // 2. 상태 검증
    if (!refundRequest.isPending()) { throw ...; }

    // 3. 포인트 잔액 검증
    if (user.getPoint() < refundAmount) { throw ...; }

    // 4. 외부 API 호출 (실패 시 예외 → 트랜잭션 롤백)
    포트원결제취소(...);

    // 5. 포인트 차감
    user.deductPoint(refundAmount);

    // 6. 상태 전이
    refundRequest.approve();
}
```

**규칙**
- 클래스 : `[이 클래스의 역할]`, `[학습 포인트]`
- 메서드 : `[동작 흐름] 1) 2) 3) ...`
- 메서드 본체 : `// 1.`, `// 2.`, ...
- **"왜" 를 설명한다**. "무엇" 은 코드 자체로 보이게.
- **학생이 헷갈릴만한 곳에 "학습 포인트" 박스** 를 추가

---

## 15. 하지 말아야 할 것 (Anti-patterns)

| ❌ 하지 말 것 | ✅ 대신 이렇게 |
|---|---|
| Spring Security 풀세트 설정 | BCrypt + 인터셉터만 |
| `System.out.println` | `@Slf4j` + `log.debug/info/error` |
| JPQL 에 문자열 결합 | `@Param` 바인딩 |
| Controller 에서 Repository 호출 | Service 경유 |
| Entity 를 Controller 반환값으로 | Response DTO |
| 비밀번호를 yml에 하드코딩 | `.env` 환경변수 |
| 서버 포트 80 | 8080 |
| 레이어 기준 패키지 (`controller/`) | 도메인 기준 (`user/`, `board/`) |
| Service 메서드명을 영어로 | 한글 OK (`회원가입()`) |
| 도메인 상태 변경을 setter 로 | 도메인 메서드 (`update()`) |
| `@OneToMany(fetch = EAGER)` | 무조건 LAZY + JOIN FETCH |
| 토큰/비밀번호 로깅 | 절대 로그에 찍지 않기 |

---

## 16. 새 프로젝트 시작 시 체크리스트

사용자가 "○○ 프로젝트 만들어줘" 라고 하면 다음을 순서대로 수행한다.

### Step 1 — 요구사항 확인 (간단히 질문)
1. 프로젝트 이름 / 패키지명 (`org.example.{name}`)
2. 어떤 도메인들이 필요한가? (예: `user`, `board`, `reply`)
3. 필요한 부가 기능? (파일 업로드, 이메일 인증, OAuth, 결제)

### Step 2 — 뼈대 생성
1. `build.gradle` (위 표준 의존성 그대로)
2. `application.yml` + `application-local.yml` + `application-dev.yml`
3. `.env.example` + `.gitignore`
4. `_core/` 전체 (config, errors, interceptor, utils)
5. `{AppName}Application.java`

### Step 3 — 도메인별 뼈대
각 도메인마다 **6개 파일 세트** 를 만든다:
1. `{Name}.java` (Entity)
2. `{Name}Repository.java`
3. `{Name}Request.java` (요청 DTO 묶음)
4. `{Name}Response.java` (응답 DTO 묶음)
5. `{Name}Service.java`
6. `{Name}Controller.java`

### Step 4 — 화면 기본 세트
1. `templates/layout/header.mustache`
2. `templates/layout/footer.mustache`
3. `templates/err/400.mustache` ~ `500.mustache`
4. `templates/index.mustache`
5. 도메인별 `list`, `detail`, `save-form` 등

### Step 5 — 시드 데이터
- `src/main/resources/db/data.sql`
- 최소한 admin 계정 1명 (BCrypt 해시된 상태로)

---

## 17. 예시 — "순수 자바 프로젝트를 스프링 부트로 이관하기"

예를 들어 "무인 편의점 재고 관리 시스템" (순수 자바 콘솔 앱) 을 이 스타일로 스프링 부트 변환한다면:

### 원본 구조 (순수 자바)
```
src/main/java
├── dto/Product.java, Admin.java, Sales.java
├── dao/ProductDAO.java, AdminDAO.java, SalesDAO.java
├── service/StoreService.java
├── view/StoreView.java
├── util/DBConnectionManager.java
└── Main.java
```

### 변환 매핑
| 원본 | 변환 후 |
|---|---|
| `dto/Product.java` | `product/Product.java` (@Entity 로 변환) |
| `dao/ProductDAO.java` | `product/ProductRepository.java` (JpaRepository) |
| `service/StoreService.java` | 각 도메인별 Service 로 분할 (`ProductService`, `SalesService`) |
| `view/StoreView.java` (콘솔 UI) | `product/ProductController.java` + `templates/product/*.mustache` |
| `util/DBConnectionManager` | 삭제 (Spring 이 관리) |
| `Main.java` | `{AppName}Application.java` (@SpringBootApplication) |
| — (없었음) | `product/ProductRequest.java`, `ProductResponse.java` 추가 |
| — (없었음) | `_core/` 인프라 추가 (예외 체계, 인터셉터) |
| — (없었음) | 관리자 로그인 → 세션 + LoginInterceptor 로 대체 |

### 도메인 재구성 (편의점 예시)
```
org.example.store
├── StoreApplication.java
├── _core/                              (표준 세트)
├── admin/                              (관리자 로그인)
│   ├── Admin.java
│   ├── AdminRepository.java
│   ├── AdminRequest.java
│   ├── AdminController.java
│   └── AdminService.java
├── product/                            (상품)
│   ├── Product.java
│   ├── ProductRepository.java
│   ├── ProductRequest.java
│   ├── ProductResponse.java
│   ├── ProductController.java
│   └── ProductService.java
└── sales/                              (판매)
    ├── Sales.java
    ├── SalesRepository.java
    ├── SalesRequest.java
    ├── SalesResponse.java
    ├── SalesController.java
    └── SalesService.java
```

### 콘솔 기능 → 웹 URL 매핑 예
| 원본 콘솔 메뉴 | 웹 URL |
|---|---|
| "상품 목록 보기" | `GET /product/list` |
| "바코드 검색" | `GET /product/list?keyword=...` |
| "상품 등록" | `GET/POST /product/save` (로그인 필요) |
| "상품 수정" | `GET/POST /product/{id}/update` |
| "상품 삭제" | `POST /product/{id}/delete` |
| "판매 처리" | `POST /sales/process` |
| "오늘 매출" | `GET /sales/today` |

---

## 18. 마지막 확인 — 결과물이 이 스타일인지 스스로 체크

코드를 완성한 뒤 다음을 전부 확인한다:

- [ ] 패키지가 도메인 기준으로 나뉘었는가? (`controller/`, `service/` 같은 레이어 폴더 없음)
- [ ] `_core/` 가 있는가?
- [ ] 예외 5종(400~500)이 전부 있는가?
- [ ] 인터셉터 3종(Session/Login/Admin)이 있는가?
- [ ] 모든 Service 에 `@Transactional` 이 있는가?
- [ ] `open-in-view: false` 인가?
- [ ] 엔티티에 setter 대신 도메인 메서드가 있는가?
- [ ] DTO 는 `{도메인}Request`, `{도메인}Response` 묶음인가?
- [ ] 비밀 값이 `.env` 로 빠졌는가?
- [ ] 주석에 `[동작 흐름] 1) 2) 3)` 패턴이 있는가?
- [ ] `System.out.println` 이 하나도 없는가?
- [ ] 서버 포트가 8080인가?
- [ ] 에러 페이지 5종이 있는가?

하나라도 ❌ 면 수정한다.

---

**끝. 이 규칙을 전부 지켜라. 사용자가 다른 요청을 해도 이 원칙과 충돌하면 사용자에게 먼저 확인한다.**
