# 📗 스프링 부트 SSR 블로그 — 최종 강의 계획서

> 대상 프로젝트: `spring_blog_22_2026` (최종 통합 단원)
> 기준: Spring Boot 3.4.12 / Java 17 / Mustache / Spring Data JPA / MySQL · H2
> 작성일: 2026-04-10
> 비고: 기존 노션의 “1~25단계(V1~V22)” 임시 순서를 대체하는 최종본. 노션 원본은 수정하지 않음.

---

## 0. 설계 철학 — 왜 다시 묶었는가

기존 노션 25단계는 `V1, V2, V3 …` 식의 “버전 누적 차분”에 가까워서, 학생 입장에서 다음과 같은 어려움이 있었습니다.

1. **단계가 너무 잘게 쪼개져** 한 차시(90분)에서 무엇을 가르치는지 흐려진다.
2. **개념 → 실습 → 회고**의 학습 사이클이 끊어진다.
3. 같은 주제(예: 인증/인가)가 5단계, 9단계, 25단계처럼 **흩어져** 있어 학생이 큰 그림을 못 만든다.
4. **“왜 이 순서로?”** 에 대한 설명이 부족 — 즉, 의존성과 학습 곡선의 근거가 약하다.

이번 계획서는 다음 4가지 원칙으로 재배치했습니다.

| 원칙 | 의미 |
|---|---|
| **개념 1개 = 차시 1개** | 한 차시에 새로운 핵심 개념을 1개만 도입한다. 라이브 코딩의 중심도 그 개념. |
| **데모는 “돌아가는 작은 것”부터** | 첫 차시부터 화면이 뜨고 글이 보이게. 추상 개념(JPA, OSIV 등)은 “필요해진 순간”에 도입. |
| **개념 → 실습 → 함정 → 회고** | 모든 차시는 같은 4단 구조. 함정(자주 하는 실수)을 명시적으로 다룬다. |
| **보안은 “마지막 한 회”가 아닌 “전 회차에 한 줄씩”** | 비밀번호 해싱, XSS, CSRF를 별도 단원이 아닌 “등장 시점”에 끼워 가르친다. |

---

## 1. 전체 커리큘럼 한눈에 보기

**총 14개 챕터(약 18~20차시 분량)**. 각 챕터는 1~2차시 분량이며, 챕터 끝에 회고/연습문제 1세트가 붙습니다.

```
PART A. 기초 다지기
 ├─ Ch01. SSR과 프로젝트 시작 (1차시)
 ├─ Ch02. 첫 화면 그리기 — 컨트롤러·뷰·정적 리소스 (1차시)
 └─ Ch03. JPA 첫걸음 — 게시글 CRUD (2차시)

PART B. 사용자와 권한
 ├─ Ch04. 회원가입과 비밀번호 해싱 (1차시)
 ├─ Ch05. 로그인·세션과 인증 인터셉터 (1차시)
 ├─ Ch06. 연관관계와 3계층 아키텍처 정착 (2차시)
 └─ Ch07. 인가와 역할 기반 접근(RBAC) — 관리자 페이지 (1차시)

PART C. 풍부해지는 게시판
 ├─ Ch08. 댓글과 페이징 (양방향/단방향 트레이드오프) (1차시)
 ├─ Ch09. 파일 업로드 — 프로필 이미지 (1차시)
 ├─ Ch10. 검색과 N+1 해결 (1차시)
 └─ Ch11. Summernote 리치 에디터와 XSS (1차시)

PART D. 결제 도메인 (이 단원의 하이라이트)
 ├─ Ch12. 이메일 인증 — 다단계 세션 패턴 (1차시)
 ├─ Ch13. 포인트와 유료 게시글 — Purchase 도메인 (1차시)
 └─ Ch14. PortOne 결제 연동과 환불 관리 (2차시)

PART E. 마무리
 └─ Ch15. 소셜 로그인(카카오) + 프로필/마이페이지 정리 (1차시)
 └─ Ch16. 보안·배포 회고 — 운영을 향해 (1차시)
```

> 💡 챕터 번호는 강의 노트의 “단원” 단위입니다. 한 챕터가 끝날 때마다 학생이 만든 코드는 **그 자체로 동작하는 작은 앱**이어야 합니다 (= 누적식 라이브 코딩).

---

## 2. 챕터별 상세 계획

각 챕터는 동일한 형식입니다.

> **🎯 학습 목표** / **🧠 핵심 개념(라이브 코딩 중심)** / **⌨️ 라이브 코딩 시퀀스** / **⚠️ 함정·자주 하는 실수** / **🧪 연습문제** / **🔐 1줄 보안 노트**

---

### Ch01. SSR과 프로젝트 시작

**🎯 학습 목표**
- “브라우저 ↔ 서버 ↔ DB”의 큰 그림을 SSR 관점에서 설명할 수 있다.
- Spring Initializr로 프로젝트를 만들고 실행할 수 있다.

**🧠 핵심 개념**
- SSR vs CSR (한 장 비유: “식당에서 음식을 받아오는 vs 재료를 받아오는”)
- `build.gradle`의 starter 개념 (web / mustache / data-jpa / lombok)
- `application.yml`과 프로파일 (`local` / `dev` / `prod`)

**⌨️ 라이브 코딩 시퀀스 (30분)**
1. start.spring.io에서 의존성 5개 선택 → 다운로드
2. IntelliJ에서 열고 `DemoSsrV11Application` 실행
3. `application.yml`에 `spring.profiles.active: local` 만 추가
4. `application-local.yml`에 H2 인메모리 + h2-console 활성화
5. `localhost:8080/h2-console` 접속해서 “DB가 연결됐다”를 시각화

**⚠️ 함정**
- IntelliJ Ultimate vs Community에 따른 yml 자동완성 차이
- Java 17 toolchain 미설정으로 인한 빌드 실패
- 포트 충돌 (`server.port: 8080`)

**🧪 연습문제**
- 포트를 9090으로 바꾸고 다시 실행하기
- `dev` 프로파일을 만들어 로그 레벨을 DEBUG로 바꿔보기

**🔐 보안 노트**
> “학습용 H2 콘솔은 절대 운영에 켜두지 말 것.”

---

### Ch02. 첫 화면 그리기 — 컨트롤러·뷰·정적 리소스

**🎯 학습 목표**
- `@Controller` + Mustache로 동적 페이지를 그릴 수 있다.
- partial(`{{> layout/header}}`)로 레이아웃을 분리할 수 있다.

**🧠 핵심 개념**
- `Model` 객체에 데이터 담기 → 뷰에서 `{{변수}}` 출력
- Mustache의 이스케이프(`{{ }}` vs `{{{ }}}`) — **여기서 XSS 첫 등장**
- `static/`와 `templates/` 디렉터리 규칙

**⌨️ 라이브 코딩**
1. `IndexController` 만들고 `/`에 `index.mustache` 매핑
2. `templates/layout/header.mustache`, `footer.mustache` 만들고 `{{> layout/header}}` 사용
3. `Model`에 `username = "ssar"` 넣고 `{{username}}` 출력
4. **일부러** `username = "<script>alert(1)</script>"`을 넣어보고 `{{ }}` vs `{{{ }}}` 차이 체험

**⚠️ 함정**
- partial 경로 오타 → 빈 화면
- `{{username}}`이 안 나오면 99% 컨트롤러에서 `model.addAttribute` 누락

**🧪 연습문제**
- 헤더에 “로그인 중: 홍길동” 같은 인삿말 출력하기

**🔐 보안 노트**
> “기본은 `{{ }}`. `{{{ }}}`는 ‘내가 직접 안전을 보장한다’고 선언하는 위험한 약속이다.”

---

### Ch03. JPA 첫걸음 — 게시글 CRUD (2차시)

**🎯 학습 목표**
- `@Entity` + `JpaRepository`로 글을 저장·조회·수정·삭제할 수 있다.
- `@Transactional`이 “언제, 왜” 필요한지 한 줄로 설명할 수 있다.

**🧠 핵심 개념**
- `@Entity`, `@Id`, `@GeneratedValue`, `@Table`
- `JpaRepository<Board, Long>`이 공짜로 주는 메서드
- 더티 체킹 (Dirty Checking) — “save 안 해도 update 되는 마법”
- PRG(Post-Redirect-Get) 패턴

**⌨️ 라이브 코딩 (1차시)**
1. `board/Board.java` (`id, title, content, createdAt`)
2. `board/BoardRepository.java` 빈 인터페이스만
3. `BoardController` — `GET /board/list`로 `findAll()`
4. `templates/board/list.mustache`에 `{{#boardList}} … {{/boardList}}` 루프

**⌨️ 라이브 코딩 (2차시)**
1. 작성: `GET /board/save` 폼 → `POST /board/save`
2. 상세: `GET /board/{id}`
3. 삭제: `POST /board/{id}/delete`
4. 수정: `GET/POST /board/{id}/update` — 여기서 **더티 체킹** 도입

**⚠️ 함정**
- `@NoArgsConstructor` 누락으로 JPA가 엔티티 생성 못 함
- `data.sql`에 한글 → `defer-datasource-initialization: true` 설명
- 수정 화면에서 `<form method="post">` 생략

**🧪 연습문제**
- “작성일이 오늘인 글만 보여주는” 메서드를 `findByCreatedAtAfter(...)`로 만들어보기

**🔐 보안 노트**
> “`title`, `content`를 출력할 때 `{{ }}` 잊지 말 것. `{{{ }}}` 쓰는 순간 XSS 문이 열린다.”

---

### Ch04. 회원가입과 비밀번호 해싱

**🎯 학습 목표**
- 비밀번호를 평문으로 저장하면 안 되는 이유를 설명할 수 있다.
- BCrypt로 해시·검증할 수 있다.

**🧠 핵심 개념**
- 단방향 해시 vs 암호화 (비유: “갈아버린 고기”)
- Salt와 레인보우 테이블 방어
- `BCryptPasswordEncoder` 빈 등록

**⌨️ 라이브 코딩**
1. `User.java` 엔티티 (`username, password, email`)
2. `WebMvcConfig`에 `PasswordEncoder` 빈 등록
3. `UserService.회원가입()`에서 `passwordEncoder.encode(...)` 호출
4. **콘솔에 BCrypt 결과 찍어보기** — 같은 “1234”도 매번 결과가 다른 것을 시각화

**⚠️ 함정**
- 회원가입 후 DB를 직접 보면 `$2a$10$...` 형태인지 확인하는 습관
- `@Column(unique = true)` 위반 시 사용자에게 “중복” 메시지 제공

**🧪 연습문제**
- `JoinDTO.validate()`에 비밀번호 길이 8자 이상 규칙 추가하기

**🔐 보안 노트**
> “DB가 통째로 유출돼도 비밀번호는 안전해야 한다. 그게 해싱의 존재 이유다.”

---

### Ch05. 로그인·세션과 인증 인터셉터

**🎯 학습 목표**
- 세션을 사용해 “로그인 상태”를 유지할 수 있다.
- 로그인이 필요한 URL을 인터셉터로 막을 수 있다.

**🧠 핵심 개념**
- `HttpSession`, `setAttribute("sessionUser", user)`
- `HandlerInterceptor.preHandle`
- 401과 403의 차이 — **여기서 커스텀 예외 등장**

**⌨️ 라이브 코딩**
1. `UserController.login()` — `passwordEncoder.matches(...)` + 세션 저장
2. **세션 ID 재발급** 한 줄 가르치기 (`oldSession.invalidate()`)
3. `_core/errors/exception/Exception401.java` 만들고 `MyExceptionHandler` 한 메서드만 추가
4. `LoginInterceptor.preHandle`에서 `sessionUser == null`이면 `throw new Exception401(...)`
5. `WebMvcConfig.addInterceptors`에서 `/board/save`만 막아보기

**⚠️ 함정**
- `request.getSession()`은 세션을 “생성”함 → `getSession(false)`의 의미
- 로그아웃은 `session.invalidate()` 한 줄로 충분
- 인터셉터 등록 안 하면 작동 안 함 (학생들이 가장 자주 빠지는 함정)

**🧪 연습문제**
- 로그인 폼에서 `autocomplete="off"` 옵션 시연 후 토론

**🔐 보안 노트**
> “로그인 직후엔 반드시 새 세션 ID를 발급하라. (Session Fixation 방어)”

---

### Ch06. 연관관계와 3계층 아키텍처 정착 (2차시)

**🎯 학습 목표**
- `@ManyToOne` 으로 “게시글의 작성자”를 표현할 수 있다.
- Controller / Service / Repository 책임 분리를 이유와 함께 설명할 수 있다.
- DTO를 왜 쓰는지(엔티티 노출 금지)를 설명할 수 있다.

**🧠 핵심 개념 (1차시)**
- `@ManyToOne(fetch = LAZY) + @JoinColumn`
- LAZY가 기본이어야 하는 이유 (성능 + 의도 명시)
- 영속성 컨텍스트와 1차 캐시 (그림으로 30초)

**🧠 핵심 개념 (2차시)**
- 3계층 아키텍처: Controller(요청 처리) → Service(트랜잭션·비즈니스) → Repository(DB)
- DTO 분리: `Request`(입력)와 `Response`(출력)
- **OSIV `false`** 의 의미 — “View가 트랜잭션을 들여다보지 못한다”

**⌨️ 라이브 코딩 (1차시)**
1. `Board`에 `@ManyToOne private User user` 추가
2. 글 작성 시 세션 사용자 → Service로 넘겨 `Board.builder().user(sessionUser)` 저장
3. 목록에서 작성자 username 표시 → **N+1을 의도적으로 발생시켜 콘솔로 보여주기**

**⌨️ 라이브 코딩 (2차시)**
1. `BoardService` 도입 → 컨트롤러에서 비즈니스 로직 옮기기
2. `BoardResponse.DetailDTO` 만들기 — 엔티티 노출 금지
3. `application.yml`에 `open-in-view: false` 켜고 LazyInitializationException 한 번 일부러 발생시키고 → DTO로 해결

**⚠️ 함정**
- `User` 엔티티에 `@Data`를 쓰면 setter가 다 열린다 — “엔티티 setter는 의미 있는 메서드(`update()`)로만 닫자”
- DTO 변환을 컨트롤러에서 하면 OSIV off일 때 폭탄

**🧪 연습문제**
- 같은 패턴으로 “내가 쓴 글 목록” 만들기 (`findByUserId`)

**🔐 보안 노트**
> “PK는 노출 표면이다. 목록에서 다른 사람 글을 ID 변조로 가져갈 수 없는지 시나리오를 떠올려라.”

---

### Ch07. 인가와 역할 기반 접근(RBAC) — 관리자 페이지

**🎯 학습 목표**
- 인증과 인가의 차이를 설명할 수 있다.
- 사용자 역할(Role)을 모델링하고 관리자 페이지를 보호할 수 있다.

**🧠 핵심 개념**
- `Role`(enum), `UserRole`(N:M 단순화)
- `AdminInterceptor.preHandle`에서 `sessionUser.isAdmin()` 검사
- 화면에서도 권한 분기 — `{{#sessionUser.isAdmin}}` (Mustache 트릭)

**⌨️ 라이브 코딩**
1. `Role.java`(USER, ADMIN), `UserRole.java`
2. `User.addRole(Role.USER)` 회원가입 시 자동
3. `AdminController` 와 `admin/dashboard.mustache`
4. `AdminInterceptor`로 `/admin/**` 보호
5. `data.sql`(또는 `CommandLineRunner`)에서 admin 시드 — **BCrypt 해시로 시드**

**⚠️ 함정**
- `@OneToMany(fetch = EAGER)` 의 N+1 리스크를 라이브로 시연 → LAZY + JOIN FETCH로 수정
- 인터셉터 순서 — Login → Admin

**🧪 연습문제**
- 관리자만 “전체 회원 목록”을 볼 수 있는 페이지 추가

**🔐 보안 노트**
> “인터셉터로 막았다고 안심하지 말고, 서비스에서도 한 번 더 검사하라.”

---

### Ch08. 댓글과 페이징

**🎯 학습 목표**
- 단방향 `@ManyToOne`만으로 댓글을 충분히 구현할 수 있다.
- `Pageable`로 페이징 UI를 만들 수 있다.

**🧠 핵심 개념**
- 양방향 vs 단방향의 트레이드오프 (이 프로젝트는 단방향 채택)
- `Pageable`, `Page<T>`, 0-based vs 1-based
- 페이징 UI에 검색어 유지

**⌨️ 라이브 코딩**
1. `Reply` 엔티티 (`comment, board, user`)
2. `ReplyController.save / delete`
3. `BoardController.boardList`에 `Pageable` 도입
4. 페이징 컴포넌트 `templates/board/list.mustache` (이전·다음·번호)

**⚠️ 함정**
- 댓글 삭제 시 IDOR — 서비스에서 `reply.isOwner(userId)` 확인 필수
- `default_batch_fetch_size: 100` 도입 시점 (지금이 적기)

**🧪 연습문제**
- 페이지 사이즈를 5/10/20 중에서 고를 수 있게 하기

**🔐 보안 노트**
> “/reply/{id}/delete의 {id}는 누구나 바꿀 수 있다. 서비스에서 소유자 검사를 절대 빼지 말 것.”

---

### Ch09. 파일 업로드 — 프로필 이미지

**🎯 학습 목표**
- `MultipartFile`로 이미지를 받아 서버에 저장할 수 있다.
- 안전한 파일 저장 패턴(UUID, 화이트리스트, 정규화)을 설명할 수 있다.

**🧠 핵심 개념**
- `multipart/form-data`, `enctype` 설정
- UUID 파일명, 디렉터리 자동 생성
- `addResourceHandlers`로 `/images/**` ↔ `file:images/` 매핑
- **확장자 화이트리스트 + 매직 넘버 검증** (보안)

**⌨️ 라이브 코딩**
1. `application.yml`의 `spring.servlet.multipart.max-file-size`
2. `_core/utils/FileUtil.java`
3. `UserService.회원가입` 안에서 파일 저장 → DB엔 파일명만
4. `WebMvcConfig.addResourceHandlers`로 화면에 표시
5. **고의로 `.html` 파일을 업로드해 보고** 차단 코드 추가

**⚠️ 함정**
- Content-Type만 믿으면 안 됨 → 확장자와 시그니처 같이 검증
- `originalFilename`에 `..\` 들어가는 경로 트래버설 시연
- SVG에 `<script>` 들어 있는 경우 — SVG는 차단

**🧪 연습문제**
- 회원정보 수정에서 “기존 이미지 삭제 후 새 이미지 저장”까지 구현

**🔐 보안 노트**
> “업로드된 폴더가 정적 리소스 경로로 그대로 노출된다는 사실을 잊지 말자.”

---

### Ch10. 검색과 N+1 해결

**🎯 학습 목표**
- `LIKE` 검색을 `@Query` + `@Param`으로 안전하게 작성한다.
- `JOIN FETCH`와 `default_batch_fetch_size`의 차이를 안다.

**🧠 핵심 개념**
- `@Query("... WHERE b.title LIKE CONCAT('%', :keyword, '%')")`
- 페이징 + JOIN FETCH의 충돌 → 일반 JOIN + 배치 페치 채택
- SQL Injection — “문자열 결합 vs 파라미터 바인딩” 라이브 시연

**⌨️ 라이브 코딩**
1. 검색 입력창 + 검색어 유지
2. `BoardRepository.findByTitleContainingOrContentContaining(...)` 직접 작성
3. `show-sql: true`로 콘솔에서 N+1 보여주고, 배치 페치 적용 후 변화 확인

**⚠️ 함정**
- `@Lob` 필드에 `LOWER()` 사용 시 CLOB 타입 오류
- LIKE `'%keyword%'`는 인덱스 안 탐 — 운영 시 한계 언급

**🧪 연습문제**
- `keyword`가 비어 있으면 전체 조회로 분기되는 코드 작성

**🔐 보안 노트**
> “JPQL이라도 `@Param` 없이 문자열 결합하면 SQL Injection이 가능하다는 사실을 잊지 말 것.”

---

### Ch11. Summernote 리치 에디터와 XSS

**🎯 학습 목표**
- WYSIWYG 에디터를 게시글 폼에 통합할 수 있다.
- 사용자가 입력한 HTML이 왜 위험한지, 어떻게 막는지 설명할 수 있다.

**🧠 핵심 개념**
- Summernote의 `onImageUpload` 콜백
- Base64 인라인 vs 서버 업로드(URL) — **장단점 비교 후 “이번에는 Base64”**
- 저장형 XSS 시연 → Jsoup `Safelist`로 살균(sanitize)
- `@Lob` (LONGTEXT)로 큰 본문 저장

**⌨️ 라이브 코딩**
1. `save-form.mustache`에 Summernote CDN 추가
2. `Board.content`에 `@Lob`
3. **(여기서 보안 한 번 크게)** 본문에 `<img src=x onerror=alert(1)>` 입력 → 저장 → 다른 계정으로 열기 → 알람 발동 → 충격 → Jsoup으로 막기

**⚠️ 함정**
- `{{{board.content}}}`를 쓰는 순간 책임은 “출력 시점”이 아니라 “저장 시점의 살균”으로 옮겨간다
- Summernote는 “안전한 HTML”을 보장하지 않는다 — 라이브러리 신뢰 금지

**🧪 연습문제**
- Jsoup `Safelist.relaxed()` 에 `img.src` 만 추가해서 우리 정책을 만들어보기

**🔐 보안 노트**
> “출력 컨텍스트가 HTML이라면, 저장 시 살균 + 출력 시 이스케이프 둘 다 한다.”

---

### Ch12. 이메일 인증 — 다단계 세션 패턴

**🎯 학습 목표**
- `JavaMailSender`로 이메일을 발송할 수 있다.
- “이전 단계 완료를 보장하는” 세션 플래그 패턴을 적용할 수 있다.

**🧠 핵심 개념**
- `spring-boot-starter-mail` + Gmail 앱 비밀번호 (**환경변수로**)
- 인증 코드 발급 → 세션에 저장 → 입력 검증 → `email_verified_<email>` 플래그
- 일회용 토큰 (인증 후 즉시 제거)
- 프론트 검증 + 백엔드 재검증의 이중 방어

**⌨️ 라이브 코딩**
1. `MailService.인증번호발송 / 인증번호확인`
2. `UserApiController` `/api/email/send`, `/api/email/verify`
3. `join-form.mustache` — 회원가입 버튼을 인증 전엔 `disabled`
4. `UserController.joinProc`에서 `session.getAttribute("email_verified_" + email)` 검증

**⚠️ 함정**
- 자격증명을 yml에 박는 순간 = 유출 — 환경변수 + `.gitignore`
- 인증 코드 세션 수명 (보통 5분) 설정 누락
- 동일 이메일 무한 재발송 방지(throttle)

**🧪 연습문제**
- 인증 코드 발송을 60초에 1회로 제한해보기

**🔐 보안 노트**
> “비밀이 git에 한 번이라도 들어갔다면, 그 비밀은 ‘유출된 비밀’이다.”

---

### Ch13. 포인트와 유료 게시글 — Purchase 도메인

**🎯 학습 목표**
- 게시글에 “유료” 플래그와 가격 개념을 도입한다.
- “포인트 차감 + 구매 내역 저장”을 한 트랜잭션으로 묶는다.

**🧠 핵심 개념**
- `Board.premium`, `User.point`
- `Purchase` 엔티티 (구매자, 게시글, 시점)
- 트랜잭션 원자성 — 차감과 저장 둘 다 실패하거나 둘 다 성공
- **동시성 문제 시연**: 두 탭에서 같은 글을 동시에 사면 어떻게 되는가?

**⌨️ 라이브 코딩**
1. `Board.premium`, `User.deductPoint(...)`, `chargePoint(...)` 메서드
2. `Purchase` 엔티티 + Repository
3. `PurchaseService.구매하기(boardId, userId)` — 한 메서드에 모든 단계
4. 상세 화면에서 “구매하지 않은 유료글 = 본문 가림” 분기

**⚠️ 함정**
- “한 트랜잭션 안의 더티 체킹”으로 포인트 변경이 자동 update 됨을 강조
- `JpaRepository.save()`를 안 호출해도 update 되는 마법을 이번엔 의도적으로 활용
- 이중 구매 방지 (이미 구매한 글에 다시 구매 호출되면?)

**🧪 연습문제**
- 구매 내역 페이지(`/user/purchases`) 만들기

**🔐 보안 노트**
> “돈을 다루는 메서드는 1) 본인 검증 2) 잔액 검증 3) 트랜잭션 4) 멱등성, 4가지를 항상 점검하라.”

---

### Ch14. PortOne 결제 연동과 환불 관리 (2차시)

**🎯 학습 목표 (1차시)**
- 외부 결제 PG의 흐름(승인 → 검증 → 저장)을 이해한다.
- 클라이언트 결과를 절대 신뢰하지 않고 서버에서 재검증한다.

**🎯 학습 목표 (2차시)**
- 환불 요청 도메인을 모델링한다.
- 관리자 화면에서 환불을 승인/거절한다.

**🧠 핵심 개념 (1차시)**
- PortOne(아임포트) `imp_uid`, `merchant_uid`
- “브라우저가 보낸 금액을 믿지 마라” — 서버에서 PortOne API로 재조회
- `RestTemplate` (또는 WebClient)로 외부 호출
- API 키는 `application.yml`이 아니라 `${IMP_REST_API_KEY}` (환경변수)

**🧠 핵심 개념 (2차시)**
- `RefundRequest` 엔티티, `RefundStatus` (PENDING / APPROVED / REJECTED / DONE)
- 본인 결제만 환불 요청 가능 (소유자 검증)
- 상태 머신 — “이미 환불된 결제에는 다시 요청 불가”
- 관리자 환불 처리 화면

**⌨️ 라이브 코딩 (1차시)**
1. `templates/user/charge-point.mustache`에 PortOne JS SDK
2. 결제 성공 → `POST /api/payment/verify` → 서버에서 PortOne API로 검증
3. `Payment` 엔티티에 `impUid, merchantUid, amount, status` 저장
4. 사용자 포인트에 `chargePoint` 호출

**⌨️ 라이브 코딩 (2차시)**
1. `RefundController` + `request-form.mustache`
2. `RefundService.환불요청화면검증`에서 IDOR 방지: `findByIdAndUserId`
3. `admin/admin-refund-list.mustache` — 관리자 승인/거절
4. 승인 시 PortOne `cancel` API 호출 + 사용자 포인트 차감

**⚠️ 함정**
- 클라이언트가 보낸 `amount`로 검증하면 안 됨 → 우리 DB의 `merchantUid` 기반
- API 키가 yml 기본값으로 박히는 사고
- 환불 진행 중 / 완료에 대한 상태 분기 누락

**🧪 연습문제**
- 결제 실패 시 사용자에게 어떤 화면을 보여줄지 토론 후 구현

**🔐 보안 노트**
> “결제는 두 번 검증한다 — 일단 PG에서, 그리고 우리 서버에서 다시.”

---

### Ch15. 소셜 로그인(카카오) + 마이페이지 정리

**🎯 학습 목표**
- OAuth 2.0 Authorization Code Grant의 흐름을 한 그림으로 설명한다.
- “로컬 사용자 vs 소셜 사용자”를 같은 도메인 안에 공존시킨다.

**🧠 핵심 개념**
- 카카오 디벨로퍼스에서 앱 등록, redirect URI
- `OAuthProvider.LOCAL / KAKAO` enum
- 인가코드 → 액세스 토큰 → 프로필 → 가입/로그인
- 임시 비밀번호도 BCrypt로 해싱
- **민감 토큰 절대 로깅 금지**

**⌨️ 라이브 코딩**
1. `application.yml`에 `oauth.kakao.client-id: ${KAKAO_CLIENT_ID}` (기본값 없이)
2. `UserController` `/user/kakao?code=...` 콜백
3. `UserService.카카오소셜로그인(code)` — 토큰 발급 → 프로필 → 사용자 생성/조회
4. 마이페이지에서 “소셜 사용자는 비밀번호/이메일 수정 불가” 분기

**⚠️ 함정**
- `redirect_uri`가 콘솔과 정확히 일치해야 함
- `RestTemplate`을 매번 `new` 하지 말고 빈으로 등록
- 카카오 토큰을 `System.out.println` 하지 말 것

**🧪 연습문제**
- 카카오 로그아웃 API 추가 호출

**🔐 보안 노트**
> “토큰은 절대 로그에 찍지 않는다. 한 번 찍으면 영원히 찍힌 것으로 본다.”

---

### Ch16. 보안·배포 회고 — 운영을 향해

**🎯 학습 목표**
- 지금까지 만든 앱을 운영에 올리기 전 점검해야 할 보안 체크리스트를 만들 수 있다.

**🧠 핵심 개념 (강의식 + 코드 리뷰)**
- 비밀 분리 (환경변수 / 시크릿 매니저)
- HTTPS, 쿠키 `HttpOnly` / `Secure` / `SameSite`
- CSRF 방어 (Spring Security 도입의 첫걸음)
- 운영 프로파일에서 H2 콘솔 / show-sql / ddl-auto 점검
- 의존성 CVE 점검 (`gradle dependencies`)

**⌨️ 라이브 코딩 (리팩터링)**
1. `application-dev.yml`의 메일 자격증명을 `${MAIL_USERNAME}/${MAIL_PASSWORD}`로 이동
2. 쿠키 보안 옵션 추가
3. 모든 `System.out.println`을 SLF4J `log`로 교체
4. (보너스) `spring-boot-starter-security` 추가 → CSRF 토큰 자동화 데모

**🧪 연습문제 / 회고**
- “내가 만든 앱에서 가장 무서운 한 줄은?” 학생 발표
- 본 프로젝트 루트의 `보안.md`를 함께 읽고 체크리스트 업데이트

**🔐 보안 노트**
> “보안은 단원이 아니라 습관이다. 다음 프로젝트의 첫 줄부터 함께 가야 한다.”

---

## 3. 차시 운영 가이드 (각 챕터 공통 90분 템플릿)

```
00:00 ─ 05:00  지난 차시 회고 + 오늘의 한 줄 목표
05:00 ─ 15:00  개념 도입 (그림 1장 + 비유 1개)
15:00 ─ 60:00  라이브 코딩 (학생 함께 따라 침)
60:00 ─ 75:00  함정·디버깅 체험 (의도적 에러 → 해결)
75:00 ─ 85:00  연습문제 (개별 또는 짝)
85:00 ─ 90:00  보안 1줄 + 다음 차시 예고
```

> 라이브 코딩 중에는 “복붙 금지, 한 줄씩 같이 친다”는 규칙을 강조. 학생들의 손이 굳어야 개념이 굳습니다.

---

## 4. 기존 노션 25단계 → 새 챕터 매핑표

기존 학습자료를 그대로 버리는 게 아니라 “재배치”라는 관점에서 매핑합니다.

| 새 챕터 | 기존 노션 단계 |
|---|---|
| Ch01 SSR과 프로젝트 시작 | 1단계 (프로젝트 구조/환경) |
| Ch02 첫 화면 그리기 | 1단계 일부 + 신규 |
| Ch03 JPA CRUD | 2단계(Entity), 3단계(Repository), V1 게시글 작성/CRUD |
| Ch04 회원가입+해싱 | (V3 사용자 도입) + 신규(BCrypt 강화) |
| Ch05 로그인/세션/인터셉터 | 4단계(예외), 5단계(인터셉터), 9단계(SessionInterceptor) |
| Ch06 연관관계+3계층 | 6단계(JpaRepository+Service), 7단계(3계층 완성), 10단계(DTO+JOIN FETCH) |
| Ch07 RBAC + 관리자 | 15단계 V12 (RBAC + 관리자) |
| Ch08 댓글+페이징 | 11단계 V8 (댓글), 12단계 V9 (페이징) |
| Ch09 파일 업로드 | 14단계 V11 (파일 업로드 + 프로필) |
| Ch10 검색/N+1 | 13단계 V10 (검색), 8단계(쿼리메서드) |
| Ch11 Summernote+XSS | 신규 (현재 코드의 Summernote 통합 단원) |
| Ch12 이메일 인증 | 19단계 V16, 25단계 V22 (검증 강화) |
| Ch13 포인트+유료글 | 20단계 V17 (Purchase) |
| Ch14 PortOne+환불 | 21단계 V18(결제), 22단계 V19, 23단계 V20, 24단계 V21(환불) |
| Ch15 소셜 로그인 | 16단계 V13 (OAuthProvider 준비) + 신규(카카오 완성) |
| Ch16 보안·배포 회고 | 17·18단계(DataInitializer 정리) + 신규(보안.md 활용) |

---

## 5. 산출물 (학습 후 학생의 손에 남는 것)

각 PART가 끝날 때마다 다음이 학생 손에 있어야 합니다.

| PART | 학생이 만든 것 |
|---|---|
| A 끝 | “게시판 1차” — 익명 CRUD가 동작하는 작은 앱 |
| B 끝 | “회원제 게시판” — 로그인/권한 + 관리자 페이지 |
| C 끝 | “블로그” — 댓글, 페이징, 검색, 프로필 사진, 리치 에디터 |
| D 끝 | “유료 콘텐츠 플랫폼” — 이메일 인증, 결제, 환불 |
| E 끝 | “출시 직전 점검” — 카카오 로그인 + 보안 체크리스트 통과 |

---

## 6. 강사 주의사항 (학생 발 빠른 함정 5가지)

1. **`@Transactional`이 빠진 service** — 더티 체킹이 갑자기 안 먹는다고 학생이 멘붕한다.
2. **`open-in-view: false`** 를 일찍 켜면 학생들이 LazyInitializationException을 자주 만난다 → Ch06에서 “일부러” 만나게 하고 DTO로 해결하는 흐름 권장.
3. **`{{{ }}}` 를 무심코 사용** — 말하면서 한 번씩 짚어주지 않으면 코드 곳곳에 박힌다.
4. **`request.getSession()` vs `getSession(false)`** — 비로그인 페이지에서 세션이 의도치 않게 생성됨.
5. **`@OneToMany(fetch = EAGER)`** — Ch07에서 한 번 EAGER를 보여주고 N+1을 시각화한 뒤 LAZY로 바꾸는 “before/after” 시연 권장.

---

## 7. 평가 (선택)

| 항목 | 비중 | 형태 |
|---|---|---|
| 챕터별 라이브 코딩 따라치기 | 30% | 출석/제출 |
| 챕터 끝 연습문제 | 30% | 코드 제출 |
| 미니 프로젝트 — “나만의 도메인 1개 추가” | 30% | 발표 |
| 보안 회고 (Ch16의 체크리스트) | 10% | 회고 노트 |

---

## 8. 다음에 이어 붙일 만한 후속 단원 (이 단원 다음 학기용)

- Spring Security 정식 도입 (Form Login → JWT → OAuth Resource Server)
- REST API 단원 — 같은 도메인을 SSR이 아닌 REST로 다시 만들기 (React 연결)
- 테스트 단원 — `@DataJpaTest`, `@WebMvcTest`, `@SpringBootTest`
- 배포 단원 — Docker / GitHub Actions / EC2

---

> 이 강의 계획은 본 프로젝트(`spring_blog_22_2026`)의 코드를 그대로 라이브 코딩 결과물로 사용할 수 있게 짜였습니다.
> 같은 폴더의 `보안.md`와 함께 사용하시면, 학생이 “기능을 만드는 능력”과 “안전하게 만드는 감각”을 동시에 가져갈 수 있습니다.
